# Що змінилося у фреймворку Altera

Читати після `deno install` — тут те, чого не видно ні в коді застосунку, ні в
скілах: що змінило поведінку й що доведеться поправити руками.

Файл розкладає в застосунок `deno task skills:sync` разом зі скілами.

---

## server 0.25.3 · client 0.13.6 · tools 0.14.9 · skills 0.1.53 · create 0.7.52

**Зовнішній агент навчився ЗАБИРАТИ файли з бази — вкладення й друковані
форми.** Досі канал байтів працював в один бік: покласти скан у базу
(`altera_attach`) було чим, а дістати його звідти — нічим. Разом із файлами не
виходив і бланк: `printPdf` не потрапляв у перелік інструментів агента взагалі,
тож на «дай накладну» агент віддавав JSON документа, а не те, що друкує кнопка
у формі.

Тепер у переліку агента дві нові речі:

- **`attachment`** — модель ядра з командами `list` і `get`. Вона й раніше
  працювала (її кличе `<ui-attachments>`), але в перелік агента не потрапляла:
  перелік збирається з манифестів, а в моделі ядра манифеста немає. Право
  звичайне — `attachment:view`;
- **`printPdf`** — у кожної моделі з непорожнім блоком `prints`. Право виводиться
  звідти ж, звідки й раніше (`view`), тож бланк доступний і токену «тільки для
  читання»: друк читає, а не пише.

Байти в контекст агента при цьому не потрапляють — цим займається обгортка
(нижче). Команда моделі віддає **підписаний токен**, а файл забирається вже ним,
власним каналом `GET /api/blob/:id?token=…`; сирий `access_key` назовні не
виходить, як і раніше.

### Потребує дії

**1. Перегенеруйте реєстр — інакше друку в агента не буде:**

```bash
deno task sql:registry
```

`printPdf` з'являється в `app/_generated/agent-tools.generated.ts`, а диспетчер
звіряє виклик саме з ним. Без цього кроку `altera_print` відмовить словами
«команда не оголошена для агента».

**2. Видайте групі право `attachment:view`** — без нього агент бачить документ і
не бачить жодного прикріпленого до нього файлу. Якщо у вашому застосунку є свій
екран груп, `attachment` у списку моделей не з'явиться: список береться з реєстру
застосунку, а модель ядра туди не входить. Рядок для ручного додавання (еталон —
`app/admin/user_group/userGroupEdit.ts`):

```ts
const CORE_MODELS = ["attachment"];
const MODEL_OPTIONS = ["*", ...[...CORE_MODELS, ...Object.keys(generatedModelRegistry)].sort()];
```

**3. Якщо користуєтесь MCP-обгорткою — оновіть її й допишіть каталог** (див.
нижче): без `ALTERA_DOWNLOAD_DIR` нові інструменти чесно відмовляють.

### Змінилося саме

- **У каталозі агента з'явився рядок `attachment` із `type: "system"`.** Шляхів
  у нього немає навмисно: свого екрана у вкладення не буде — воно показується
  всередині форми власника, тож посилання вело б у нікуди.
- **Запис у вкладення агенту не відкрито.** `save` (прив'язка) і `delete` до
  переліку не входять: прив'язка — робота форми, яка щойно зберегла запис, а
  видалення файлу незворотне й не має ані позначки, ані `confirm`. Коли
  знадобиться — це буде окреме рішення, а не дописаний рядок.
- **Застосунок сильніший за ядро.** Інструменти ядра підмішуються ПЕРЕД
  застосунковими, тож модель із тим самим іменем у вашому манифесті лишається
  головною.

#### Обгортка MCP: два нові інструменти (`mcp@0.3.0`)

Обгортка публікується окремо від фреймворку (D6) і в цей реліз не входить —
беруть її з репозиторію; після правки конфіга хост треба перезапустити.

| інструмент | що робить |
|---|---|
| `altera_fetch` | вкладення запису (або одне за id) на диск |
| `altera_print` | друкована форма запису в PDF на диск |

```json
{ "model": "invoice", "id": "42" }
```

**Файл віддається шляхом, а не вмістом — в обидва боки.** Це те саме рішення, що
й у `altera_attach`, лише дзеркально: бланк на 50 КБ у base64 коштував би 70 КБ
у КОЖНІЙ розмові, де до нього повернулися. Виняток один і навмисний — **мініатюра
зображення** (~10–50 КБ): шлях каже, ДЕ файл, і не каже, ЩО це, а серед вкладень
накладної буває і скан акта, і фотографія пломби. PDF мініатюри не має —
рендерера в обгортці немає й не буде.

**Каталог називає людина, а не агент.** Нові поля запису MCP-хоста:

```json
"env": { "ALTERA_URL": "…", "ALTERA_TOKEN": "…", "ALTERA_DOWNLOAD_DIR": "C:/altera-files" },
"args": ["run", "--allow-net", "--allow-env", "--allow-read", "--allow-write=C:/altera-files", "jsr:@altera/mcp@^0.3.0"]
```

Не задано `ALTERA_DOWNLOAD_DIR` — файли не забираються взагалі, і у відмові
сказано, що дописати. Аргументом «куди покласти» це не зроблено свідомо: дозвіл
`--allow-write` виданий один на процес і вибором в інструменті вже не звужується,
зате файли розповзлися б по диску. Наявні файли не затираються (`акт (2).pdf`) і
не прибираються — каталог належить людині.

**`altera_call` більше не тягне PDF у контекст.** `printPdf` можна покликати й
ним — це звичайна команда моделі, — і тоді в розмову лягала б уся друкована форма
в base64, яку агент не може ні відкрити, ні зберегти. Тепер байти звідти
зрізаються, а на їхньому місці стоїть вказівка на `altera_print`.

#### `client 0.13.6` їде без нових можливостей

У номері 0.13.5, який уже лежить у реєстрі, немає плагіна нотисів
(`THIRD-PARTY-NOTICES.md` поруч із бандлом): він доїхав у `client/` наступним
комітом, тобто після публікації. Це той самий випадок, який стереже крок
`scaffold:verify` — «вміст проти реєстру», — і лікується він рівно бампом. Для
застосунку зміна одна: після оновлення `@altera/client` збірка кладе в `dist/`
файл із ліцензіями того, що бандлер справді поклав у вихід.

Заразом полагоджено два `clearTimeout(undefined)` у базових класах: під
@types/node така форма не типізується, і публікація пакета впала б на ній. На
поведінці це не позначається ніяк.

---

## client 0.13.5 · skills 0.1.52 · create 0.7.51

**Довідник, у якого батьком запису є ТАКИЙ САМИЙ запис, нарешті показується
деревом — `ModelTreeListBase` і `ModelTreePickerBase`.** Доти дерево у
фреймворку було рівно одне — панель ГРУП (`hierarchy: true`), де папка є
рядком окремої таблиці `{model}_group` і в проводку потрапити не може. Для
номенклатури це правильно, а для підрозділів (батько дільниці — ЦЕХ, і саме
він іде в субконто), статей затрат і статей доходів — ні, і вибір стояв так:
або чесна модель даних без дерева в списку, або дерево ціною неправди про
структуру.

```ts
// список
export class SubdivisionList extends ModelTreeListBase<SubdivisionRow> {
  protected model = "subdivision";
  protected editRoute = "catalog/subdivision/edit";
  protected columns = [ /* як завжди */ ];
  // за потреби: treeParentKey = "parentId", treeColumn, treeRowLimit, treeIndent
}

// діалог підбору — те саме, але від ModelTreePickerBase
```

Від моделі потрібні дві речі й жодного рядка SQL: `parent_id bigint references
app.{model}(id)` у `struc.sql` і `parentId` у RowSchema. Згенерований `list`
підходить як є — у режимі дерева основа просить НАБІР ЦІЛКОМ (`page: 1`,
`pageSize` = `treeRowLimit`, умовчання 5000) і перегруповує рядки за батьком
на клієнті. Наслідок, який варто знати одразу: **окремого «сортування дерева»
не існує** — глобальне сортування сервера стає порядком БРАТІВ І СЕСТЕР
усередині кожного вузла.

Керування вузлами з коду — дієслова, а не перемикач: `expandNode(id)` /
`collapseNode(id)` / `toggleNode(id)`, `expandAll()` / `collapseAll()` і
`revealNode(id)`, який розгортає шлях до вузла й ставить туди курсор. У пікері
`revealNode` — головний сценарій: показати вже вибране значення поля.

Три межі названі свідомо: **діючий пошук тимчасово перемикає в звичайний
плоский список із пагінацією** (знайдений вузол без предків показувати
нечесно), очищення пошуку повертає дерево; **вивантаження в Excel плоске**;
**набір, обрізаний стелею, називає банер** — неповне дерево не вдає повне.
`hierarchy: true` у підкласі дерева елементів валить старт: два механізми
взаємовиключні, і мовчазне поєднання було б гіршим за помилку.

**Випадне вікно, що розкривається ЗБОКУ, тепер має свій розрахунок —
`computeSidePlacement` / `placeSidePopover`.** Доти ядро вміло розміщувати
вікно ПІД полем (`placePopover` — пікер, календар, період), а «збоку від
пункта» кожен малював сам: підменю згорнутої рейки, контекстне меню рядка,
розшифровка комірки звіту.

```ts
import { placeSidePopover } from "@client/ui-kit/popover.ts";

protected override updated() {
  if (!this.flyout) return;
  const el = this.renderRoot.querySelector<HTMLElement>(".flyout");
  if (el) placeSidePopover(el, this.flyout.anchor);   // після рендера!
}
```

Правила: верх вирівнюється по якорю, але вікно цілком лишається на екрані —
надто низьке **підіймається**, а не обрізається; вище за екран стає під верхній
відступ і прокручується саме; по горизонталі — праворуч від якоря, а біля
правого краю дзеркалиться вліво.

### Потребує дії

**Якщо ваше меню має підменю згорнутої рейки — воно, найпевніше, зламане так
само.** Ознака: у довгому меню нижні пункти підменю йдуть за нижній край
екрана, і дістати їх нічим — вікно `fixed`, прокрутка сторінки під ним не рухає
нічого.

Причина в тому, що координата бралася з пункта як є (`top: rect.top`).
Лікується трьома правками: тримати в стані САМ пункт (`anchor: HTMLElement`), а
не його `y`; прибрати `top` із розмітки вікна; покликати `placeSidePopover` в
`updated()`. Висоту підменю видно лише після того, як воно потрапить у DOM, —
тому саме після рендера, а не в обробнику кліку.

Це той самий випадок, від якого застерігає опис `popover.ts`: **друга копія
розміщення в застосунку повторює ту саму помилку, лише без притискання до краю
екрана**. Тепер копію можна прибрати.

### Змінилося саме

- **У діалозі підбору з'явився банер повідомлень** — між тулбаром і таблицею,
  той самий, що в списку. Це не частина дерева: доти відмова сервера в
  `lookup` була невидимою в КОЖНОМУ пікері — діалог просто лишався порожнім.
  Якщо ваш пікер малює щось своє над таблицею, перевірте, що місця вистачає.
- **Плоскі списки й пікери не змінилися ні на крок** і за дерево не платять:
  механіка живе в окремому модулі, який підключають лише нові базові класи.
- **`placePopover` не змінився ні на крок** — під полем усе як було.
- **Обмеження висоти скидається**, коли місця вистачає: розміщення кличуть на
  кожне оновлення, і `max-height`, поставлений раз, лишався б назавжди.
- **Меню шаблону scaffold цього не стосується** — у ньому немає ані згортання,
  ані підменю.

---

## skills 0.1.51 · create 0.7.50

Тільки текст скілів — коду не змінилося нічого.

**Вибір періоду одиницею описаний там, де вибирають форму фільтра**, а не серед
пасток. У `model-list-filters` це окремий розділ «A question asked in whole
units» одразу після таблиці анотацій, а `model-report-form` посилається на нього
з місця, де про фільтри й ідеться: звіт, поставлений місяцем, — саме звітний
випадок.

### Потребує дії

Нічого. Але якщо ви вже додали фільтр за місяцем між `client 0.13.4` і цим
релізом — перевірте анотацію: вона мусить бути **звичайна `"x-filter": true`**,
а не `{ "op": "range" }`. Рука тягнеться до `range`, бо контрол зветься
періодом, а діапазон дозволив би спитати за півтора місяця — питання, на яке
довідка закриття відповісти не може. Цього в тексті першої редакції сказано не
було.

---

## server 0.25.2 · client 0.13.4 · tools 0.14.8 · skills 0.1.50 · create 0.7.49

Чотири записи прикладників: дві прогалини підпорядкованого регістру, вибір
періоду одиницею й ширини колонок бланка, названі числами.

### Підпорядкований регістр

Обидві прогалини — з тих, що не падають.

**Перейменований відбір за датою називається явно — `dateFilterKey`.** «Перейти
до дати» шле `<основа>From` / `<основа>To`, і основою було саме ім'я поля. Але
`x-filter` уміє відбір перейменувати — `{ "op": "range", "key": "date" }` на полі
`period` дає `_list` ключі `dateFrom` / `dateTo`, — і форма ця не екзотична: такі
імена віддає `<ui-period>`, і саме їх несе `DocumentHeaderSchema` фреймворку.
Тобто два механізми ядра вимагали від одного відбору різних імен.

```ts
dateField: "period",       // яким полем ходить перехід
dateFilterKey: "date",     // під яким іменем його бачить _list (x-filter.key)
```

Значення — та сама ОСНОВА, що в `x-filter.key`, а не готовий ключ: відбір за
датою це пара, і одним рядком її не назвати.

**Порожня панель може сказати, що діє ЗАМІСТЬ записів — `emptyKey`.** Порожній
періодичний регістр означає не «нічого немає», а «діє умовчання», і умовчання в
кожного своє: без курсів документи візьмуть 1:1, без облікової політики —
загальна система й 20 % ПДВ, без цін ціна в документ не підставиться. Ключ
локалізації поруч із `titleKey`, показується замість `common.noData`.

### Вибір періоду ОДИНИЦЕЮ — `<ui-period units="month">`

Питання ставлять не лише відрізком: «закриття за березень», «декларація за
I квартал», «баланс за 2025-й». Пресети цього не давали — усі вісім прив'язані
до сьогодні, тож щоб узяти березень у серпні, лишалося набрати дві дати руками,
тобто рівно той ввід, від якого поле й рятує.

```html
<!-- лише місяці: смуги вкладок немає — вибір з одного пункту не вибір -->
<ui-period units="month" value="2026-03-01"></ui-period>

<!-- одиниці на вибір, плюс довільний відрізок окремою вкладкою -->
<ui-period units="month,quarter,year,custom"></ui-period>
```

У поповері замість пресетів — смуга одиниць, навігатор `‹ 2026 ›` і сітка з 12
місяців, 4 кварталів або 12 років. Порядок у `units` значущий: він задає порядок
вкладок і одиницю за умовчанням; невідоме слово пропускається з попередженням у
консоль, а не мовчки.

**Значення лишається парою.** `value` — початок одиниці; він не замінює
`dateFrom`/`dateTo`, а розгортається в них, тож те саме поле годиться і звіту,
що фільтрує однією датою, і звіту, що фільтрує парою. Приводиться до початку
одиниці: 17 серпня, покладене в поле місяця, означає серпень — і прочитається як
`2026-08-01`.

### Ширини колонок бланка: ядро каже, кому скільки дісталося

`minPt` — єдине число, яке в схемі намірів лишається за прикладником, і добирали
його перебором **повних прогонів PDF**, міряючи висоту таблиці як непрямий
показник «комусь стало тісно». Крива при цьому не монотонна, тож «підняти й
подивитися» не працює, а висота однаково не каже, КОМУ забракло.

Рядок таблиці у звіті про розкладку тепер несе `columns`:

```ts
const { layout } = await renderPrintPdfWithLayout(template, data);
for (const column of layout.find((b) => b.key === "section_b")?.columns ?? []) {
  column.widthPt;    // що дісталося — те саме число, яким малювали
  column.minPt;      // підлога: найдовше СЛОВО або оголошений minPt
  column.naturalPt;  // скільки треба, щоб не переносилося взагалі
  column.atMin;      // дісталася рівно підлога
}
```

Пара `minPt`/`naturalPt` відповідає на питання прямо: нижче підлоги колонка не
стиснеться, вище природної ширини піднімати немає сенсу. Тобто замість двадцяти
восьми прогонів достатньо одного.

### Потребує дії

**Якщо у вашій панелі перехід до дати «нікуди не веде», перевірте ім'я відбору
першим.** Промах тут не падає й нічого не пише в консоль: невідомий ключ
згенерований `_list` ПРОПУСКАЄ, тож лічильник «скільки рядків по той бік дати»
повертає всі рядки, і перехід стає на першу сторінку — рівно так, ніби він
відпрацював. Дивіться в схему підпорядкованої моделі: є `"key"` в `x-filter`
поля дати — назвіть його ж у `dateFilterKey`.

Обхід, який на це вже зробили (зняти `key` зі схеми, щоб імена збіглися),
скасовувати не обов'язково — він працює. Але він забирає у списку тієї ж моделі
штатні `dateFrom` / `dateTo`, тобто панель `<ui-period>` там доведеться
перекладати руками; повернувши `key` і назвавши `dateFilterKey`, обидві сторони
дістають своє.

### Змінилося саме

- **Умовчання не змінилося ні на крок.** Немає `dateFilterKey` — основою
  лишається `dateField`, як було; немає `emptyKey` — стоїть `common.noData`.
- **`dateFilterKey` без `dateField` не вмикає нічого**: поле дати в смузі дій
  показує саме `dateField`, і fail-closed тут той самий.
- **`<ui-period>` без `units` не змінився**: поповер із вісьмома пресетами й
  парою `ui-date` той самий, `value` поза режимом одиниці не діє — розгортати
  нема чим.
- **Подія `period-changed` дістала третє поле — `unit`.** Одиниця, якій період
  дорівнює РІВНО, або `null` для довільного відрізка; наявні обробники читають
  `dateFrom`/`dateTo` і не помічають різниці.
- **`columns` немає в таблиці на відсотках.** Підлоги й природної ширини там не
  рахує ніхто, а нулі замість них були б неправдою саме про ті числа, по яких
  добирають `minPt`.

---

## server 0.25.1 · tools 0.14.7 · skills 0.1.49 · create 0.7.48

**Дворівневу шапку бланка можна написати переліком граф, а не `colSpan`.**
Затверджена форма майже завжди має шапку на два рівні: «Номери» над трьома
графами, «Дорогоцінні метали» над трьома, решта граф займає обидва. На двадцяти
чотирьох графах (ОЗ-6) це двадцять чотири числа, кожне з яких помиляється ТИХО:
сітка з'їде на графу, шаблон лишиться валідним, публікація зеленою, а
розбіжність побачить той, хто звірятиме папір із бланком.

```json
"columns": [
  { "key": "c_inv",    "width": "4%",   "header": "Номери", "headerSub": "інвентарний" },
  { "key": "c_serial", "width": "5.5%", "header": "Номери", "headerSub": "заводський" },
  { "key": "c_year",   "width": "3%",   "header": "Рік випуску (побудови)" }
]
```

Об'єднання не задають — воно **виводиться**: сусідні графи з однаковою `header`
склеюються в комірку верхнього рівня, графа без `headerSub` займає обидва рівні,
перелік без жодного `headerSub` дає однорядкову шапку. Це той самий крок, що
зроблено з ширинами (0.24.0): шаблон називає намір, геометрію рахує ядро.

У редакторі шаблонів графа й підграфа — два поля в рядку колонки; показуються
вони, лише поки секція шапки порожня.

### Потребує дії

Нічого. Поля необов'язкові, наявні бланки не змінилися ні на піксель: коротка
форма діє, лише коли `sections.header` ПОРОЖНЯ, а написана руками шапка сильніша
за виведену завжди.

### Змінилося саме

- **Склеюються лише СУСІДНІ графи.** Порядок колонок — це порядок граф у формі,
  тож той самий напис на початку бланка й у кінці лишається двома об'єднаннями,
  а не збирається в одне через півсітки.
- **Комірки виходять по центру й напівжирними.** Об'єднана комірка над трьома
  графами, притиснута ліворуч, не схожа на жоден затверджений бланк; колонка,
  якій треба інакше, каже `headerAlign` / `headerFontWeight` — вони діють і тут.
- **Схована колонка (`visibleWhen`) виведеній шапці не шкодить**: `colSpan` план
  рендеру перераховує сам, так само як для написаної руками.

---

## server 0.25.0 · tools 0.14.6 · skills 0.1.48 · create 0.7.47

**Розрив сторінки в бланку — `pageBreakBefore`.** Способу сказати «звідси —
новий аркуш» не було взагалі: типу блока «розрив» немає, а прапорці, передані в
розміщенні, зникали в нормалізаторі мовчки.

```json
{ "key": "back_title", "type": "text", "value": "Зворотний бік акта",
  "pageBreakBefore": true,
  "placement": { "mode": "flow", "gapPt": "0", "xPercent": "0", "widthPercent": "100" } }
```

Це НАМІР, і його не замінює ніщо з наявного: потік переносить блок, коли той не
влізв; `keepTogether` тримає групу вкупі; евристика підвалу спрацьовує від
залишку місця. Затверджена двобічна форма вимагає протилежного — зворотний бік
починається з нового аркуша ЗАВЖДИ, хоч би лицьовий зайняв півсторінки. Таких
форм в українському обліку багато: НА-1, НА-3, НА-4, М-2, інвентаризаційні описи
з розпискою на звороті, акти приймання-передачі.

Читається лише в режимі `flow` (як `keepTogether`) і не діє на ПЕРШОМУ блоці
бланка: порожній перший аркуш — це не розрив, а зламаний друк. У редакторі
шаблонів — прапорець «З нового аркуша» поруч із «Не відривати від наступного».

### Потребує дії

**Якщо у вашому застосунку є екран редактора шаблонів** (`app/admin/print_template/`),
його треба поправити: `PrintTemplateBlock` дістав обов'язкове поле, і
`createBlock` без нього не пройде перевірку типів. У кожен літерал
`printTemplate.blocks.ts` — рядок поруч із `keepTogether`:

```ts
keepTogether: false,
pageBreakBefore: false,
```

Прапорець у самому редакторі (`printTemplateEdit.ts`) не обов'язковий, але без
нього ознаку можна буде поставити лише в JSON шаблону. Обидва файли в
монорепозиторії фреймворку оновлені — їх можна взяти звідти цілком, разом із
ключем `printTemplate.pageBreakBefore` у локалях екрана.

**Підняти піни** `@altera/server@^0.25.0` (обидва рядки — пакет і `/sql`),
`@altera/tools@^0.14.6`, `@altera/skills@^0.1.48`.

---

## client 0.13.3 · skills 0.1.47 · create 0.7.46

**Підпорядкований регістр уміє другий вимір ключа — `scope`.** Панель відбирала
рядки рівно за одним власником, а ключ регістру відомостей рідко буває
одновимірним: у картці основного засобу це «організація × основний засіб ×
дата», у рахунків обліку номенклатури — «організація × позиція». Довідник один
на базу, а відомості про його об'єкт ведуться в розрізі організації.

Наслідків було два, і обидва закривалися в застосунку: у перелік приходили рядки
ВСІХ організацій (людина мусила розрізняти чужі очима), а новий рядок діставав
від панелі лише поле власника — тобто не записувався взагалі, доки застосунок не
клав організацію туди своїм `createRow`. Обхід коштував колонки-пікера
організації в кожній панелі — колонки, яка існувала не тому, що її треба
правити.

```ts
private amortization = new SubordinateRegister<FaAmortizationRow>(this, {
  model: "fa_amortization",
  ownerField: "fixedAssetId",
  ownerId: () => this.$root.item.id,
  scope: () => ({ organizationId: currentOrganization.id }),   // ← оце
  columns: [ /* організації тут більше немає */ ],
});
```

Ключі — **поля рядка**, а не ключі відбору: значення робить дві роботи одразу —
їде у відбір `list` і лягає в рядок. Ключ відбору виводиться з поля тією ж
конвенцією, що й у власника (`organizationId` → `organization`, значення
`{ id }`); поле без суфікса `Id` іде у відбір як є.

### Потребує дії

Нічого — поле необов'язкове, наявні панелі працюють як працювали. Але якщо ви
обходили це колонкою-пікером організації, приберіть її разом із організацією в
`createRow`: **розріз записується завжди**, а не лише при створенні, тож панель
однаково перепише поле своїм значенням. Записується завжди навмисно — правка,
яка вивела б рядок із розрізу, дала б рядок, що зник із панелі, де його щойно
правили.

Кожне поле розрізу мусить нести в схемі підпорядкованої моделі
`"x-filter": true` — та сама вимога, що до поля-власника; без неї згенерований
`_list` відповість «невідомий фільтр».

### Змінилося саме

- **Порожнє значення розрізу робить панель НЕ готовою**, а не «без цього
  відбору»: показати в цей момент увесь перелік означало б показати чужі рядки —
  рівно те, від чого розріз рятує, і саме тоді, коли цього не чекають (шапка
  застосунку ще не догрузилася). Підказка називає причину окремим текстом
  (`core.subordinate.scopeNotSet`), а не відсилає зберігати вже збережену картку.

---

## tools 0.14.5 · client 0.13.2 · skills 0.1.46 · create 0.7.45

Три виправлення одним релізом. Номери проміжних версій (`tools` 0.14.4,
`client` 0.13.1) не існують у реєстрі й не існуватимуть: публікація трьох
попередніх комітів упиралася в чужу залежність, молодшу за добу
(`@rolldown/binding-darwin-x64` — політика мінімального віку), а коли вона
розв'язалася, у реєстр поїхало все разом. Кожне з трьох описане окремо нижче.

### Ссылка на «будь-який документ» несе код свого типу

`x-ref: { entity: "document" }` показував реєстратора, але
вкладений об'єкт мав лише те, що є в шапці `app.document`:

```json
"document": { "id": "1419", "presentation": "Введення в експлуатацію №ДМВЕ-000001 …" }
```

Тип документа лежить окремою таблицею (`app.document_type.code`), і код цей і є
ключем моделі, з якого виводиться маршрут в'ю. Тобто рядок знав, ЯКИЙ документ
його поставив, і не знав, ЯКОГО той типу: колонка з реєстратором виглядала як
посилання й вела в нікуди, а обійти це можна було лише зайвим запитом ПЕРЕД
кожним переходом. Тепер в об'єкті є `typeCode`, і перехід — два рядки:

```ts
const doc = row.document;
const route = doc && viewRoute(doc.typeCode, "edit");
if (route) bus.emit({ type: "tab.open", route, id: doc.id });
```

Код приходить тільки з `entity`: ссылка, що назвала `model`, і так знає модель —
код у неї сталий, він у самому оголошенні.

**Своя кнопка в смузі дій підпорядкованого регістру.** Комірку дій малює панель,
тож поставити в неї свою дію не було куди взагалі — вона виносилася окремою
колонкою (3rem заради одного значка; подання документа посиланням не помістилося
б, воно довге за побудовою). Слот — `rowActions` у конфігурації, ліворуч від пари
«правка / видалення»: комірка притиснута до правого краю, тож пара лишається на
місці й у рядках без своєї дії.

Маршрут складає ЗАСТОСУНОК, і не з делікатності: `viewRoute` читає
`view-manifest`, згенерований з ВАШИХ манифестів, а фреймворк його не бачить —
залежність іде в один бік.

#### Потребує дії

**`typeCode` приходить зі згенерованого SQL**, тож моделі з
`x-ref: { entity: "document" }` треба перегенерувати й опублікувати:

```bash
deno task sql:gen <family>/<model>
deno task sql:assemble && deno task sql:deploy --yes
```

І дописати ключ у схему — інакше типи про нього не знають:

```ts
document: Type.Optional(Type.Union([
  Type.Object({ id: Type.String(), presentation: Type.String(), typeCode: Type.String() }), Type.Null(),
], { "x-transient": true })),
```

**Підняти піни** `@altera/tools@^0.14.5`, `@altera/client@^0.13.2` (обидва рядки),
`@altera/skills@^0.1.46`.

#### Змінилося саме

- **Join під вкладений об'єкт більше не губиться.** Об'єкт ссылки виводиться
  разом зі СВОЇМ полем, тож модель, що назвала в `RowSchema` `documentId` і не
  назвала `document`, однаково діставала у вивід `'document', … r_document.…`, а
  join під нього не будувався. Генерація й публікація мовчали (тіло plpgsql при
  створенні не перевіряється), і падало це аж на першому виклику —
  `missing FROM-clause entry for table "r_document"`.

### Смуга вкладок форми переноситься, коли не вміщається

`.form-tabs` у темі була рядком без переносу й без власної прокрутки. Доки вкладок дві-три
(табличні частини документа — той випадок, під який компонент робився), це не
було видно; дев'ять українських назв дають смугу ширшу за форму — і далі вона
РОЗПИРАЄ форму, горизонтальна прокрутка з'являється в усієї сторінки, а гортання
вкладок зсуває вбік панель під ними: людина шукає дев'яту вкладку, а їде
таблиця. У вужчому вікні те саме виходить і на трьох вкладках, тож питання було
не в кількості, а в тому, що поведінки на випадок «не вміщається» не було.

Прокрутки в смуги немає навмисно: смуга — це ПЕРЕЛІК частин, і схована вкладка
для людини не існує; до того ж обрізання по осі X тягне обрізання по Y, а
вкладка навмисно вилазить на 1px за межі смуги — її лінія лягає на лінію смуги.

#### Потребує дії

**Якщо ви обходили це своїм правилом — приберіть його.** Обхід виглядав так
(у `app/styles/tailwind.css`, з подвоєним класом, бо тема дописується після
файлу застосунку):

```css
.form-tabs.form-tabs { flex-wrap: wrap; }
```

Тепер це в темі, і правило застосунку діє на всі форми з вкладками, включно з
документами, — наступний, хто його там побачить, не знатиме, чому воно є.

Піни — ті самі, що в першій частині: цей реліз один.

### `sql:registry` не випускає порожні схеми агента мовчки

Заразом **`solution:update` ставить залежності сам**: обидва — про одне місце,
ланцюжок оновлення поставки на щойно розгорнутій установці.

`update-solution` друкував правильний порядок («`deno install`», далі задачі), а
виконував його з другого рядка. Тобто `sql:registry` запускався без
установлених залежностей — і в такому стані не падав: жодна схема моделі не
резолвилася, кожна віддавала порожньо, і на диск лягав
`app/_generated/agent-tools.generated.ts` розміром 14 КБ замість 376 КБ, з кодом
виходу нуль. Наслідків два, обидва тихі: установка отримувала агента **без
інструментів** (екрани працюють, MCP-обгортка бачить порожньо), а поставка
одразу читалася як «ЗНЯТО З ПІДТРИМКИ» — ланцюжок переписував файл, чию
контрольну суму сам щойно записав у `.solution.json`.

#### Потребує дії

**Підняти пін інструментів у `deno.json`** — задачі належать каркасу, самі вони
не приїдуть ніколи. Замінити `@altera/tools@^0.14.3` на `@altera/tools@^0.14.5`
в усіх рядках, де він стоїть (`sql:registry`, `sql:gen`, `locales:build`,
`token`, `solution:export`, `solution:import`, `solution:status`,
`solution:update`, і в `imports` — `"@altera/tools/"`), а `@altera/skills` — на
`^0.1.46`.

**Якщо установку вже оновлювали через `solution:update`, звірте стан.** Ознака
описаного вище — `deno task solution:status` каже «змінено 1:
`_generated/agent-tools.generated.ts`», хоча рішення ніхто не правив. Лікується
тим самим ланцюжком, тепер уже в правильному порядку:

```bash
deno install
deno task sql:registry
deno task solution:status   # має бути «НА ПІДТРИМЦІ»
```

#### Змінилося саме

- **Генератор реєстру ВІДМОВЛЯЄТЬСЯ писати, коли схема моделі не
  завантажилася.** Розрізняються два випадки, які доти ловив один `catch`:
  схеми **немає** (модель із рукописним SQL, admin-екран без TypeBox) — це не
  помилка й ніколи нею не було; схема **є, але не завантажилася** — тепер
  відмова з поіменним переліком моделей і причиною. Не завантажилася жодна —
  у тексті прямо названо `deno install`. Генерація або проходить цілком, або не
  міняє нічого: попередні файли в `_generated/` лишаються на місці.
- **Так само з шапкою документа.** `@client/shared/schema.ts`, що не
  завантажилася, доти давала мовчазний `null` — інструменти документів
  виходили без дати, організації й номера, тобто агент не міг створити жодного.
  Тепер це відмова, якщо в застосунку є хоч один документ.
- **`solution:update` виконує `deno install` першим кроком, ще до
  розпакування.** До розпакування — щоб невдала установка залежностей лишила
  рішення цілим: піни живуть у `deno.json` каркаса, з пакета тут не ставиться
  нічого. Заразом інструмент більше не друкує ланцюжок, який сам же й виконує.
- **Політику мінімального віку залежності інструмент називає, а не обходить.**
  На свіжій установці `deno.lock` ще немає, піни резолвляться з реєстру, і
  реліз фреймворку, молодший за добу, блокується. Раніше це валилося посеред
  ланцюжка, на `sql:deploy`, коли файли в `app/` уже записані, а текст помилки
  нічого не пояснював. Тепер відмова приходить першим кроком, установка ціла, а
  в повідомленні стоїть `deno install --min-dep-age=0` — рішення знімати
  політику лишається за адміністратором.

#### Тільки для нових застосунків

У `Dockerfile` і `docker-compose.yml` шаблону версія оновлювача вказана точно,
без `^`. Хто вже має свою копію — міняє `jsr:@altera/tools@0.14.3/update-solution`
на `@0.14.5` в обох файлах.

---

## tools 0.14.3 · skills 0.1.43 · create 0.7.42

**Поставку рішення можна взяти з релізу GitHub, а не лише з файлу на диску.**
`solution:import` і `solution:update` приймають у тому самому аргументі три форми
джерела, і вид виводиться з вигляду рядка, а не з прапорця:

```bash
deno task solution:update -- ./erp-1.2.0.tar.gz              # файл, як було
deno task solution:update -- https://…/erp-1.2.0.tar.gz      # прямий URL
deno task solution:update -- IgorAIvanov/erp@1.2.0           # реліз GitHub
deno task solution:update -- IgorAIvanov/erp@latest          # останній реліз
```

Форма `власник/репозиторій@версія` — шов, а не зручність: коли поставки переїдуть
у окремий каталог застосунків, зміниться те, у що вона резолвиться, а не рядок,
який набирає адміністратор.

### Потребує дії

**Щоб цим користуватися, у наявному застосунку треба підняти пін і додати
`--env-file`.** Обидві правки — в `deno.json`, у рядках задач; автоматично вони
не приїдуть ніколи, бо задачі належать каркасу.

Було:

```json
"solution:import": "deno run -A jsr:@altera/tools@^0.14.2/import-solution",
"solution:update": "deno run -A --env-file jsr:@altera/tools@^0.14.2/update-solution",
```

Стало:

```json
"solution:import": "deno run -A --env-file jsr:@altera/tools@^0.14.3/import-solution",
"solution:update": "deno run -A --env-file jsr:@altera/tools@^0.14.3/update-solution",
```

`--env-file` тут заради `GITHUB_TOKEN`: без нього приватний репозиторій не
відкриється, а токен природно живе в `.env` установки поруч із рештою
налаштувань. Решта пінів `@altera/tools` у задачах піднімається так само —
`^0.14.3`.

**Приватний репозиторій — рядок у `.env`:**

```
GITHUB_TOKEN=ghp_…      # право читання того репозиторію; GH_TOKEN теж приймається
```

Без токена GitHub відповідає **404, а не 403** — приватний репозиторій він не
показує навіть фактом існування. Тому «релізу не знайдено» на завідомо вірний тег
означає саме брак токена, і команда так і каже.

### Змінилося саме

- **`@latest` — це останній РЕЛІЗ, а не найсвіжіший тег**: чернетки й передрелізи
  GitHub сюди не віддає. Для поставки це те, що треба.
- **Ассет не вибирається сам, якщо їх кілька.** Умовчання — єдиний `.tar.gz`;
  інакше інструмент перелічує знайдене й вимагає `--asset <ім'я>`. Архіви «Source
  code», які GitHub чіпляє до релізу завжди, поставкою не є: у них немає
  `solution.json`.
- **Завантажений пакет прибирається після роботи.** Тримати його ні до чого:
  повернутися до попередньої версії тепер означає НАЗВАТИ її
  (`власник/репозиторій@1.1.0`), а не шукати архів на диску.
- Локальний шлях проходить наскрізь без жодної дії — стара форма поводиться
  точно як раніше.
- Новий експорт `@altera/tools/resolve-solution-source` — для того, хто складає
  свій конвеєр поставки: `resolveSolutionSource(spec)` віддає локальний шлях і
  спосіб його прибрати.

### Тільки для нових застосунків

Шаблон scaffold отримав `--env-file` у задачі `solution:import` і згадку про
джерела в описі розділу `//solution`. Наявні застосунки цього не отримають —
рядок для ручного додавання наведено вище.

---

## client 0.13.0 · skills 0.1.42 · create 0.7.41

**Підпорядкований регістр правиться в сітці, а не в смузі редактора.** Панель тепер
виглядає й керується як таблична частина документа: смуга дій угорі, правка прямо в
рядку, той самий контракт комірок і та сама клавіатура. Окрема смуга над таблицею
прибрана: вона повторювала ту саму сітку другим разом і змушувала оголошувати кожне
поле двічі.

### Потребує дії

**Хто вже поставив `SubordinateRegister` (client 0.12.6) — прибрати `fields`.** Показ і
правку тепер описує ОДНА колонка: `kind` переїжджає в неї, `required` і `precision`
теж. Тип `SubordinateField` більше не експортується, а зайвий `fields` у конфігурації
не компілюється — мовчки старий код не поїде.

Було:

```ts
columns: [
  { key: "period", title: "currencyRate.period", width: "8rem", format: dateFormat.date },
  { key: "rate",   title: "currencyRate.rate",   width: "8rem", align: "right" },
],
fields: [
  { kind: "date",    key: "period", title: "currencyRate.period", required: true },
  { kind: "decimal", key: "rate",   title: "currencyRate.rate", precision: 6, required: true },
],
```

Стало:

```ts
columns: [
  { kind: "date",    key: "period", title: "currencyRate.period", width: "8rem",
    format: dateFormat.date, required: true },
  { kind: "decimal", key: "rate",   title: "currencyRate.rate",   width: "8rem",
    precision: 6, required: true },
],
```

Колонка, яку показують, але не правлять (реєстратор, обчислена сума), позначається
`readonly: true` — інакше вона стане редагованою коміркою.

**Умовчання `pageSize` — 10 замість 50.** Перелік у картці не журнал: курсів валюти за
десять років тисячі, і панель, яка вивалює п'ятдесят рядків, з'їдає форму власника.
Якщо у вашій картці записів свідомо небагато й потрібне старе вікно — задайте
`pageSize: 50` явно.

### Змінилося саме

**Правка йде в рядку.** Рядок, який відкрили, перетворюється на контроли в тих самих
комірках; решта таблиці лишається текстом. Клавіатура: `Insert` — новий рядок, `Enter` —
записати, `Esc` — відмовитися, подвійний клац — відкрити рядок. Смуга дій —
«Створити / Правити / Видалити», як над табличною частиною; дії дивляться на поточний
рядок.

**Межа запису лишилася явною.** Рядок у правці — це чернетка, і на сервер вона їде
натисканням (✓ або `Enter`), а не «сама», коли курсор пішов з рядка. Причина не в
звичці: пише команда чужої моделі, яка може відмовити (дубль періоду, закритий період),
а мовчазний запис по виходу з рядка втрачав би недописаний рядок при першому ж
перемиканні вкладки.

**Перелік гортається сторінками.** Під таблицею — та сама пагінація, що в списку моделі
(«‹ 3 / 13 ›» плюс загальна кількість), і показується вона, лише коли сторінок справді
більше однієї. Кількість береться з `totals.count` того самого конверта `list`, тож
окремої команди не треба. Сторінка, що зникла під ногами (останній рядок видалили —
тут або в сусідній вкладці), відступає на наявну, а не показує порожнечу посеред
непорожнього переліку.

**«Перейти до дати» веде до найближчого ЗАПОВНЕНОГО запису.** Оголосіть
`dateField: "period"`, і в смузі дій з'явиться поле дати. Перехід ставить сторінку на
запис, що діяв на цю дату, і виділяє його; дата, старша за весь регістр, веде на
найстаріший рядок. Порожніх дат у регістрі не буває за побудовою: значення діє З дати й
до наступного запису, тож рядка рівно на названий день може не бути — а найближчий
заповнений є завжди.

Це **сторінка, а не відбір**: вибірка лишається повною, тож від дати можна гортати в
обидва боки — саме заради цього до неї й переходять. Позиція рахується двома лічильними
викликами `list` (`pageSize: 1`, потрібне лише `totals.count`), край діапазону панель
вибирає за напрямом сама: `periodTo` при спаданні (умовчання), `periodFrom` при
зростанні.

**Що для цього треба в моделі:** поле дати мусить нести `"x-filter": { "op": "range" }`
(після правки — `sql:gen` і публікація). Без `dateField` поле не показується взагалі:
кнопка, за якою немає відбору, гірша за відсутню.

**Комірки — контракт табличної частини** (`.table-tabular` / `cell-control`), тож
однакові на вигляд таблиці на сусідніх екранах більше не мають двох різних способів
вводу. Поточний рядок і невалідна комірка підсвічуються так само, як там.

**Чотири рішення, заради яких панель у ядрі, не змінилися**: запис одразу, вимкнена
панель на незбереженій картці, відбір за іменем ссылки, рядок документа не правиться з
картки.

## client 0.12.6 · skills 0.1.41 · create 0.7.40

**Підпорядкований регістр малюється в картці власника компонентом ядра.** «Значення X
для Y, починаючи з дати» — курс валюти в картці валюти, ціна в картці номенклатури,
ознака «платник ПДВ» в обліковій політиці організації, параметри амортизації в картці
основного засобу. Записи живуть окремою моделлю зі своїм екраном, і це правильно, але
людина, яка відкрила картку, шукає їх саме там.

### Потребує дії

**Нічого.** Це нова можливість; наявні екрани не змінюються.

Якщо ви малювали таку панель самі — її можна замінити оголошенням:

```ts
private rates = new SubordinateRegister<CurrencyRateRow>(this, {
  model: "currency_rate",
  ownerField: "currencyId",
  ownerId: () => this.$root.item.id,
  titleKey: "currencyRate.titleMany",
  sortBy: "period",
  readonly: () => this.readonlyMode,
  columns: [
    { key: "period", title: "currencyRate.period", width: "8rem", format: dateFormat.date },
    { key: "rate",   title: "currencyRate.rate",   width: "8rem", align: "right" },
  ],
  fields: [
    { kind: "date",    key: "period", title: "currencyRate.period", required: true },
    { kind: "decimal", key: "rate",   title: "currencyRate.rate", precision: 6, required: true },
  ],
  createRow: () => ({ id: "", period: "", rate: 0, multiplicity: 1 }),
});
// render(): <ui-subordinate-register .register=${this.rates}></ui-subordinate-register>
```

**Одна річ на боці моделі**: поле-власник у схемі підпорядкованої моделі потребує
`"x-filter": true`, інакше згенерований `_list` відбору не знає й відповість «невідомий
фільтр». Після правки схеми — `sql:gen` і публікація.

### Змінилося саме

**Контролер плюс в'ю, як у табличної частини**: `SubordinateRegister` тримає стан і дії
(завантажити, додати, правити, видалити), `<ui-subordinate-register>` малює панель —
заголовок, «Створити», порожній стан, редактор рядка. Замінити подання своїм можна
будь-коли: усі дії публічні на контролері.

**Чотири рішення, заради яких це в ядрі**, і в кожному помиляються тихо:

- **запис іде ОДРАЗУ, а не разом із карткою** — регістр це окрема модель зі своїми
  перевірками, і «разом» означало б двофазний запис, який ламається посередині;
- **у нової картки ще немає id** — панель вимкнена, і про це сказано словами;
- **відбір іде за ІМЕНЕМ ССЫЛКИ** (`currency`), а не поля (`currencyId`): панель
  виводить це з `ownerField`, відкидаючи суфікс `Id`; інша назва — `ownerFilterKey`;
- **рядок, поставлений документом, не правиться з картки** — документ перепише його
  начисто при перепроведенні. Умовчання — заповнений `documentId`, інше правило —
  `lockedWhen`.

**Колонки й поля оголошуються типізовано в коді форми, а не в манифесті.** Причина та
сама, що в табличної частини: ключі перевіряє компілятор. Друга — граф чанків: щоб
вивести редактор зі схеми чужої моделі, її схема мусила б приїхати в бандл разом із
реєстром, тобто кожен застосунок платив би за схеми ВСІХ своїх моделей на кожному екрані.

### Тільки для нових застосунків

Еталон — `app/catalog/currency/currencyEdit.ts` (курси валют у картці валюти) — живе в
демо-застосунку цього репозиторію й у scaffold не їде. Опис із прикладом цілком —
у скілі `model-form-root`.

---

## server 0.24.0 · tools 0.14.2 · skills 0.1.40 · create 0.7.39

**Ширини колонок бланка тепер рахує ядро, а колонка оголошує НАМІР.** Досі, щоб
проставити відсотки, застосунок мусив проробити роботу рендерера: зміряти найдовше
слово кожного значення, рознести підписи шапки по реальних колонках, повторити
перенос по словах і роздати лишок тим колонкам, які тримають висоту. На бланку з
дев'ятнадцяти колонок сума «потрібного» склала 970.5 pt при аркуші 761.9 — і без
перерозподілу шапка розганялася на 11 рядків замість 6.

### Потребує дії

**Нічого — якщо ваші бланки на відсотках.** Розвилка в рендері стоїть на «чи оголосив
хоч ХТОСЬ намір»: доки всі колонки задані `widthPercent`, розкладка та сама, до пункта.
Це під пробою окремим випадком.

**Перевірте бланки, де ви розставляли дефіси руками.** Слово, ширше за комірку, тепер
РОЗРИВАЄТЬСЯ, а не вилазить на сусідню колонку, — і явний `\n` у тексті комірки
поважається. Якщо ви обходили це, вставляючи пробіли з дефісами прямо в підпис
(«сіль- сько- госпо- дарська»), обхід можна зняти:

```json
{ "text": "власна сільсько-\nгосподарська\nпродукція" }
```

Самі дефіси нікуди не поділися й далі працюють — але тепер це вибір, а не єдиний спосіб.

### Змінилося саме

**Три наміри замість числа:**

```json
"columns": [
  { "key": "num",  "width": "fit" },
  { "key": "name", "width": "auto", "minPt": "60" },
  { "key": "vat",  "width": "8%" }
]
```

`fit` — рівно стільки, щоб значення не переносилося (числа, коди, дати); `auto` —
забирає лишок; відсоток — як було завжди. `minPt` — нижня межа в пунктах. Непорожній
`width` перекриває `widthPercent` тієї ж колонки; `widthPercent` лишається чинним і
нікуди не дінеться.

Ділиться це трьома кроками: відсотки забирають своє; якщо решті вистачає на «не
переноситися» — кожен дістає рівно стільки, а лишок іде колонкам `auto`; якщо не
вистачає — кожен дістає свій мінімум (найдовше СЛОВО), а надлишок роздається за
правилом висоти: крок іде колонці, чия висота зараз найбільша. Правило прислали
прикладники, і воно взяте як є.

**Що рахівник знає такого, чого не знала прикидка в застосунку.** Об'єднана комірка
каже про СУМУ своїх колонок, а не про кожну окремо (позиційна прикидка саме тут і
брехала — на тризначній шапці з `rowSpan` показувала чужі числа); повернутий текст
`textOrientation: "90"` росте вгору, тож ширини йому треба на один рядок; явний розрив
зменшує потрібну ширину, а не лише вигляд; і рахуються ВСІ рядки, а не сама шапка —
інакше колонка з числами на 12 знаків отримала б ширину слова «Сума».

**Межа, названа явно.** Буває, що не влазять навіть найдовші слова — дев'ятнадцять
підписів затвердженої форми дають суму мінімумів, більшу за альбомний аркуш. Ядро тоді
стискає пропорційно, а слово ріже перенос: розрив у МЕЖАХ комірки, а вихід за межі не
має жодного правильного прочитання.

**Правило переносу стало спільним** — `wrapPrintText` у `@altera/server/print/metrics`.
Читачів у нього два: рендерер, який малює, і рахівник ширин. Дві копії розійшлися б
мовчки — ширина рахувалася б за одним переносом, а на папір ліг би інший.

**Редактор шаблонів (тільки цей репозиторій).** Поле ширини колонки приймає `auto`,
`fit` і відсоток, поруч з'явилося поле нижньої межі в пунктах. У вашому застосунку
екран редактора свій — щоб дати адміністраторові ці два поля, правити треба його.

---

## server 0.23.3 · tools 0.14.1 · skills 0.1.39 · create 0.7.38

**Шар обчислень навчився вимагати ВІДСУТНОСТІ виміру.** `p_dims` працює на входження:
задані пари мають бути присутні. Сказати ним «договору немає» не можна було, і той,
кому це потрібно, відсіював рядки поіменно вже після шару — читаючи `dims`, тобто через
широкий вхід `acc_entries`, тобто платив за складання jsonb-аналітики на кожен рух. У
розрахунках із контрагентами це найгарячіше місце системи: сальдо кличе кожна оплата,
надходження й відвантаження.

### Потребує дії

**Опублікуйте схему** — правка в SQL ядра (`@core/ledger`):

```bash
deno task sql:assemble && deno task sql:publish
```

**Якщо ви обходили це читанням `dims`** — тепер обхід можна зняти. Було:

```sql
from app.acc_entries(org, null, p_date, array['361'], jsonb_build_object('counterparty', cp)) e
where p_contract_id is not null
   or not exists (select 1 from jsonb_array_elements(e.dims) d
                  where d->>'dimensionCode' = 'contract')
```

Стало — вузький вхід і відбір цілком у шарі:

```sql
from app.acc_entries_agg(org, null, p_date, array['361'],
       jsonb_build_object('counterparty', cp)
         || case when p_contract_id is null then '{}'::jsonb
                 else jsonb_build_object('contract', p_contract_id::text) end,
       case when p_contract_id is null then array['contract']::varchar[] end) e
```

На стенді це 634 мс проти 2 971 на 20 живих викликах (~4.7×); відповідь та сама
рядок-у-рядок — перевірено проти самого обходу.

### Змінилося саме

**`p_absent varchar[]` — шостий параметр `acc_entries` і `acc_entries_agg`**, з
умовчанням `null`. Перелік вимірів, яких на боці бути НЕ повинно. Наявні виклики не
міняються: п'ять аргументів працюють як працювали, і план у них той самий (заміряно —
планування 13.6–15.0 мс проти 14.5–15.3 до правки).

Окремим параметром, а не парою `{"contract": null}` у `p_dims`: один об'єкт не може
нести дві протилежні вимоги, а розділення ще й дає тому, хто відсутністю не
користується, передати літеральний `null` — і підзапит зникає з його плану разом зі
своєю вартістю.

**Дві межі, і обидві виміряні, а не з обережності.** Відсутність УТОЧНЮЄ відбір:
`p_absent` без жодної пари в `p_dims` не підходить під жодну половину й дає порожній
результат. Причина — «відсутність без присутності» входила б перебором усіх боків, і
її оцінка (1.4 млн) сиділа б у КОЖНОМУ плані шару, вмикаючи JIT-компіляцію всім, хто
цим не користується. І `acc_journal` відсутності не приймає взагалі: там рядок це
проводка, а не бік, і відбір означає «хоч один бік несе всі пари».

**Пастка, варта окремої згадки, бо вона не про цю правку.** Перша версія передавала
обчислений `p_absent` в обидві половини `union all`. Мертва половина не виконувалася —
і живий виклик подорожчав із 5 мс до 1 240. У плані все було чисте: `One-Time Filter`
на місці, мертве піддерево `never executed`. Час ішов на JIT: оцінка мертвої половини
перевалила `jit_inline_above_cost`, і PostgreSQL компілював 97 функцій для гілки, яку
не виконує. Тобто правило «усе, що вирішується прапорцем, мусить вирішуватися
ЛІТЕРАЛОМ» сильніше, ніж здавалося: літерал потрібен не лише щоб мертва гілка не
вплинула на вибір плану, а й щоб вона не мала ВАРТОСТІ. Подробиці й числа —
`docs/ledger-performance.md` у репозиторії фреймворку.

---

## server 0.23.2 · tools 0.14.0 · skills 0.1.38 · create 0.7.37

**Незнайомий ключ у `commands` манифеста більше не проходить мовчки.** Досі команда,
оголошена на рівень вище (`"commands": { "fill": … }` замість
`"commands": { "sql": { "fill": … } }`), не потрапляла в реєстр узагалі — і не казав
про це ніхто: ані генератор, ані `deno task check`, ані публікація SQL. Функція в базі
при цьому є й працює, тож усі проби зелені; видно тільки тому, хто натисне кнопку.

**І друге, з тієї ж родини мовчазних відмов:** `x-ref` умів показати документ лише
однієї названої моделі, а регістр відомостей пише кілька — рядок від другого писаря
в join не знаходився, і колонка «Документ» була порожня без жодної скарги. Тепер ціль
можна назвати сутністю: `"x-ref": { entity: "document" }`.

### Потребує дії

**Виконайте `deno task sql:registry` після оновлення `@altera/tools`** — і прочитайте,
що він скаже. Генератор тепер валиться на двох речах:

```
app/document/vat_compensating/manifest.json: блок "commands" не буде прочитаний так, як написаний:
  "fill" — невідомий ключ. Команду оголошують у "commands.sql" (SQL-функція) або "commands.ts" (TS-хендлер), її право — у "commands.access".
  "access.fill": "vat_compensating.update" — такої дії рантайм не знає. Дозволені: view, create, edit, delete, post, unpost, authenticated.
```

Обидва випадки означають, що команда **вже не працювала** — генерація лише перестала
про це мовчати. Правильна форма:

```json
"commands": {
  "sql": { "fill": "vat_compensating_fill" },
  "access": { "fill": "edit" }
}
```

Читати всередині `commands` генератор уміє рівно три ключі — `sql`, `ts`, `access`.
Значення `access` — одна з дій прав (`view` / `create` / `edit` / `delete` / `post` /
`unpost`) або `authenticated`; рядок виду `"model.update"` не задовольнить жодне право,
що існує, тобто це помилка запису, а не сувора політика.

**Якщо у вас є кнопка, яка «нічого не робить»** — цим оновленням її причина, найпевніше,
назветься сама. Ознака та сама, за якою це знайшли: SQL-функція в базі є, демо-набір
кличе її напряму й проходить, а з екрана та сама команда дістає відмову.

### Змінилося саме

**`x-ref` уміє показати БУДЬ-ЯКИЙ документ, а не одну названу модель.** Регістр
відомостей на те й регістр, що в нього пише кілька документів: місцезнаходження
основного засобу ставить і введення в експлуатацію, і переміщення; стан — введення,
списання, передача. Ссылка, що називає одну модель, join'иться через її таблицю, тож
рядок, записаний ДРУГИМ документом, у ньому не знаходиться — і колонка «Документ»
мовчки порожня, тобто екран каже неправду: «цей рядок завели руками».

```ts
documentId: Type.Optional(Type.Union([Type.String(), Type.Null()], {
  title: "Документ", "x-db-type": "bigint",
  "x-ref": { entity: "document", as: "document" },
})),
document: Type.Optional(Type.Union([Type.Object({}), Type.Null()], { "x-transient": true })),
```

Join тоді одинарний — прямо в `app.document`; подання те саме (`presentation` з
падінням на номер). Ціль називають **рівно одним способом**: `model` або `entity`,
обидва разом і жодного — помилка генерації.

Дві межі, названі відразу. Показати можна **лише колонку спільної шапки** (`number`,
`doc_date`, `total`, `presentation`, `description`, `is_posted`, `is_deleted`,
`organization_id`) — власних реквізитів у «будь-якого документа» немає за
визначенням, і `display` поза цим переліком відхиляється на генерації. **Пікера над
журналом документів це не дає**: `<ui-picker url="…">` іменує маршрут в'ю, а журнал —
екран застосунку, і фреймворк його за нього не оголосить. Для реєстратора регістра це
не заважає — колонку заповнює проведення, а не руки.

Ознака, за якою цю прогалину шукати в себе: FK у базі стоїть, `select` по таблиці
показує `document_id`, а список моделі показує порожньо. Ані генерація, ані `check`,
ані публікація не заперечують — join валідний, він просто не про той документ.

**Перевірка стоїть у генераторі реєстру, бо перелік ключів знає він один.** Манифест —
звичайний JSON і прийме будь-що; прочитаний буде тільки той ключ, який генератор шукає.
Тому мовчання тут коштувало не хвилин на пошук, а тижнів: три документи ПДВ прожили з
непрацездатною кнопкою від дня, коли їх зробили, до дня, коли хтось порівняв два
манифести очима.

**Словник дій перевіряється копією, і це свідомо.** Канонічний перелік стоїть
check-constraint'ом на `app.user_group_permission.action`; копія в генераторі можлива
рівно тому, що розбіжність із нею падає ГОЛОСНО — з'явиться нова дія, і генерація
почне лаятися на чинний манифест одразу, а не мовчатиме.

**Відмова рантайму 404 тепер називає ключ манифеста, а не лише факт.** Було «Команду
«model/command» не налаштовано.» — тепер той самий текст плюс те, куди дивитися:
`commands.sql` / `commands.ts`, право в `commands.access`, після правки `sql:registry`.
Найчастіша причина цієї відмови — не друкарська помилка в імені команди, а форма запису
вище.

---

## server 0.23.1 · tools 0.13.25 · skills 0.1.37 · create 0.7.36

**Токен «тільки читання» більше не пише вкладення.** Якщо ви видавали такі токени
зовнішнім агентам, вважаючи, що вони не можуть змінити базу, — до 0.23.1 могли:
запис байтів обходив запобіжник.

### Потребує дії

**Оновіть `@altera/server` — правка серверна, SQL не чіпає.** Публікувати схему не
треба, досить `deno install` і перезапуску.

**Перегляньте вкладення, залиті токенами**, якщо у вас були видані read-only токени
й ви покладалися на них як на межу:

```sql
select a.id, a.name, a.size, a.created_at, a.owner_model, a.owner_id
from app.attachment a
order by a.created_at desc;
```

Прапорця «залито токеном» у таблиці немає, тож звірка тут очима — за часом і
власником. Це найнеприємніша частина: слідів рівно стільки, скільки в звичайного
завантаження.

### Змінилося саме

**Запобіжник токена став властивістю ТОКЕНА, а не одного споживача.** Він стояв у
`ModelRuntimeService.execute`, і в документації було сказано, що обійти його іншим
входом не вийде. Виходило: байти вкладень ходять власним каналом
(`POST /api/blob/upload`), а той про прапорець не знав — токеном для читання можна
було залити будь-який файл до ліміту розміру. Тепер перевірка живе поруч із рештою
помилок межі запиту (`assertTokenMayWrite`, `ReadOnlyTokenError` у
`server/common/http.ts`), стоїть ДО читання тіла запиту (інакше сервер приймав би
десятки мегабайт, щоб потім їх відкинути) і віддає 403.

Решта входів перевірена: `/api/auth/*` пускає лише сесію (токеном туди не зайти —
саме тому токен не може викарбувати собі новий), решта запису йде рантаймом
моделей. Правило для наступного входу записане в CLAUDE.md: пише — кличе
`assertTokenMayWrite`.

**Під пробою.** Крок smoke «агент: токен, його межі й підтвердження змін» тепер
перевіряє і другий вхід: той самий read-only токен, яким уже перевіряється відмова
`save`, дістає 403 на завантаження файлу. Перевірено, що крок падає без виправлення.

#### Обгортка MCP навчилася вкладенням (`mcp@0.2.0`)

Четвертий інструмент — `altera_attach`: скан накладної до документа, підписаний
оригінал до акта.

```json
{ "path": "C:/scan/накладна.pdf", "model": "goods_receipt", "id": "1032" }
```

Обгортка публікується окремо від фреймворку (D6) і в цей реліз не входить —
беруть її з репозиторію; після правки конфіга хост треба перезапустити.

**Файл називається шляхом, а не вмістом**: аргументи інструмента лишаються в
контексті агента, і накладна на 420 КБ у base64 коштувала б 560 КБ у КОЖНІЙ
розмові, де до неї повернулися. Зворотний бік названий у README прямо — обгортка
читає будь-який файл, доступний користувачу; хто з цим не згоден, звужує
`--allow-read` до каталогу.

**Власник обов'язковий**: запис має існувати ДО прикріплення (`save`, потім
`attach` до отриманого id). Вкладення без власника — «сирота», і його за добу
прибирає `attachment_gc`; у браузері власника проставляє збереження форми, а
агенту тієї форми нема де зберігати.

Показати вкладення людині — і далі робота застосунку: байти лягають у
`app.attachment`, але на формі їх видно лише там, де стоїть `<ui-attachments>`.

---

## server 0.23.0 · tools 0.13.24 · skills 0.1.36 · create 0.7.35

Вертикаль бланка більше не треба вгадувати. Блок може стати ПОТОКОМ — під
попереднім, хай де той скінчився, — а `yPercent` тепер означає ВЕРХ блока в усіх
типів однаково. Обидва записи ваші: і той, де підпис поруч із клітинками
підганяли на око, і той, де висоту таблиці доводилось передбачати числом.

### Потребує дії

**Опублікуйте SQL — половина цього релізу у функціях ядра:**

```bash
deno task sql:assemble && deno task sql:publish
```

**Переведіть підсумкових споживачів на `acc_entries_agg`.** Це той окремий вхід,
який ви просили: той самий відбір і та сама методологія, але без `dims`,
`corr_dims`, назв рахунків, типу документа й коду валюти. Усе, що РАХУЄ, а не
показує рядки, має кликати його:

```sql
-- було
from app.acc_entries(p_organization_id, null, p_date_to, '{281}', p_dims) e
-- стало
from app.acc_entries_agg(p_organization_id, null, p_date_to, '{281}', p_dims) e
```

Кандидати з вашого коду названі у вашому ж записі: `inventory_balance`,
`settlement_balance`, `settlement_offset`, `inventory_document_qty`,
`inventory_sold_cost`. Колонок у вузького входу менше, тож перехід не мовчазний —
запит із `e.dims` просто не скомпілюється, і це навмисно.

**Порядку у вузького входу немає.** Сортувати те, що зараз згорнеться в суму, —
робота на порожньому місці. Потрібен порядок — беріть `acc_entries`.

**Передивіться бланки — текст з'їде вниз на висоту літери** (0.75 кегля: 6 pt
при кеглі 8). Там, де текст стоїть сам по собі — заголовок, реквізити,
підписи, — це непомітно й правити нічого не треба.

**Правити доведеться там, де підпис вирівнювали ДО ЧУЖОЇ РАМКИ** — до поля по
клітинках, до картинки, до таблиці. Така підгонка робилася на око саме тому, що
блоки стояли по різні боки однієї координати; тепер вона зайва:

```json
{ "key": "date_caption", "type": "text",       "placement": { "yPercent": "18.4"  } }
{ "key": "date_cells",   "type": "char-cells", "placement": { "yPercent": "16.78" } }
```

стає двома однаковими `yPercent` — блоки, які мусять стояти поруч, тепер несуть
ОДНЕ число:

```json
{ "key": "date_caption", "type": "text",       "placement": { "yPercent": "16.78" } }
{ "key": "date_cells",   "type": "char-cells", "placement": { "yPercent": "16.78" } }
```

**Шаблон, збережений у редакторі, лежить у базі — файл його не замінить.** Сід
іде `on conflict do nothing`, тож правити такі бланки треба в редакторі шаблонів
(вкладка PDF тепер показує те саме, що полотно). Файли в `prints/**` правляться
як звичайно.

### Змінилося саме

#### Шар обчислень: аналітика окремим входом

**`acc_entries_agg` — рухи без аналітики.** Правило «що вважається рухом»
лишилося сказаним один раз: обидва входи стоять на спільному тілі, і різниця між
ними рівно в тому, що коштує. На стенді (80 тис. проводок, 6 000 рухів на
гарячій номенклатурі) ОСВ із відбором 61 → 28 мс, залишки по партіях 320 →
119 мс.

**Широкий вхід теж подешевшав** — 94 → 37 мс: аналітика проводки читається
одним заходом замість двох (свій бік і кореспондуючий розводить `filter`), а
словник вимірів більше не сканується на кожен рух. Живий виклик із обчислюваним
`p_dims` — 105 → 43 мс.

**Назва рахунку приєднується після групування** в `acc_balance_turnover`,
`acc_balance` і `acc_turnover`, а розріз за субконто в
`acc_balance_turnover_by_dims` береться одним заходом в аналітику замість
розбирання повного списку на кожен рух.

Відповідь шару не змінилася ніде: всі 14 випадків стенда збігаються біт-у-біт
(`ledger-compare.sql` → `ALL MATCH`). Числа, плани й три пастки, на яких ця
правка спіймалася дорогою, — у `docs/ledger-performance.md` вашого боку не
стосуються, але одна з них варта згадки й тут: **усе, що вирішується прапорцем,
мусить вирішуватися літералом**. Параметр, який керує гілкою або join'ом, на
живому шляху не константа — і гілка лишається в кожному плані.

#### Розкладка потоком

```json
{ "key": "section_b_caption", "type": "text", "value": "Розділ Б",
  "placement": { "mode": "flow", "gapPt": "10", "xPercent": "0", "widthPercent": "100" } }
{ "key": "section_b", "type": "table",
  "placement": { "mode": "flow", "gapPt": "4", "xPercent": "0", "widthPercent": "100" } }
```

`mode: "flow"` ставить блок під ПОПЕРЕДНІМ У СПИСКУ `blocks`, хай де той
скінчився. Змішувати з координатами можна й треба: шапка відповідає затвердженій
формі до міліметра й лишається абсолютною, а все від першої таблиці й нижче йде
стосом. Абсолютний блок теж рухає курсор, тож стос законно починається під
шапкою.

- **Блок, який не влазить, іде на наступну сторінку цілим.** `keepTogether` («не
  відривати від наступного») тримає на одній сторінці цілу групу — цим
  виражається «підпис не відривати від твердження», замість порога в 64 pt, який
  вгадував те саме по відстані між блоками. Таблиця ознаки не слухає: вона
  розривається сама, по записах.
- **`yPercent` у потоці лишається й нічого не вирішує на папері** — це місце
  рамки на полотні редактора, і поле там так і підписане.
- **Досить одного блока з `flow`, щоб евристика підвалу вимкнулась на ВСЬОМУ
  бланку.** Це те правило, через яке підпис між двома таблицями їхав на другий
  аркуш разом із підвалом: усе, що нижче ПЕРШОЇ таблиці, вважалося підвалом.
  Бланк без жодного `flow` працює як працював — щоб наявні шаблони не поїхали.

#### Куди що стало

```ts
import { renderPrintPdfWithLayout } from "@altera/server/print/render";

const { bytes, layout } = await renderPrintPdfWithLayout(template, data);
layout.filter((block) => block.overflow);            // що не влізло
layout.find((block) => block.key === "section_b");   // де саме стала таблиця
```

Ключ, тип, сторінка початку й кінця, `topPt`/`bottomPt` (координати PDF — вісь y
росте вгору) і `overflow`. Ядро знало це в момент малювання й доти викидало, а
ви з'ясовували своє місце розпакуванням потоку готового PDF. `renderPrintPdf`
лишився як був — віддає самі байти.

#### Координати блока

**`yPercent` — верх блока, для всіх типів.** Вміст стоїть під цією межею:
рамка з клітинок висить під нею, картинка й таблиця починаються з неї, текст
відсунутий від неї вниз на висоту літери. Доти в `text` і `field-list` `y`
означала базову лінію ПЕРШОГО РЯДКА, тобто літери стирчали над рамкою, — два
блоки з однаковою `yPercent` опинялися по різні боки координати, на цілу висоту
блока. Полотно редактора при цьому малювало текст УСЕРЕДИНІ рамки, тобто
показувало не те, що піде на папір; тепер вони збіглися.

**Порожня висота в полі по клітинках означає КВАДРАТ** — клітинку заввишки в
саму себе завширшки (було: кегль × 1.8, не менше 14 pt). Це не зручність:
відсотки по двох осях беруться від різних сторін аркуша, тож той самий квадратик
записується двома різними числами (13 pt — 2.52 % завширшки й 1.71 % заввишки),
і виходило, що квадратик доводилось рахувати. Рамку розміряють від клітинки —
`widthPercent = count × розмір ÷ 5.1528`, звідки 12 клітинок по 13 pt — 30.3 %.
Задана висота сильніша: клітинка затвердженої форми буває й видовженою.

#### Замір бланка

**`printContentHeight()` — друга сторона області друку** в
`@altera/server/print/metrics`, поруч із `printContentWidth()`. Потрібна саме
через різні знаменники: розмір, однаковий по обох осях, у розкладці
записується двома різними числами, і пробі бланка треба вміти порахувати обидва.

**Невідома орієнтація тепер валить замір, а не мовчить.** Обидві функції
приймають рівно `"portrait"` / `"landscape"`; зайвий аргумент
(`printContentWidth("A4", "landscape")`) доти означав книжкову — 515.28 замість
761.89, — і неправильними ставали всі ширини, пораховані від цього числа, без
жодної ознаки, крім паперу.

---

## server 0.22.1 · tools 0.13.23 · skills 0.1.35 · create 0.7.34

Індексний вхід у `acc_entries` нарешті вмикається там, де ви його кличете. Ви
заміряли проведення після 0.21.3 і сказали, що воно не впало — це виправлення
саме про це.

### Потребує дії

**Опублікуйте SQL — зміна цілком у функціях ядра:**

```bash
deno task sql:assemble && deno task sql:publish
```

Пакет оновиться, а база лишиться зі старими функціями, і мовчки: жодної помилки,
просто колишня швидкодія.

**Заміряйте свій найдорожчий виклик ще раз.** Ми міряли обгорткою, яка повторює
ваш `inventory_balance`; у вас на місці й партійний облік, і документи, яких на
стенді немає. Якщо час проведення знову не впав — це знову до нас, і знову з
планом.

### Змінилося саме

**Мертва гілка `union all` більше не псує оцінку.** Індексний вхід (0.21.2) у
плані був і виконувався, але поруч лишалася гілка «без відбору»: вона
відсікалася на ВИКОНАННІ одноразовим фільтром, а на ПЛАНУВАННІ входила в
`Append` усіма своїми рядками. Планувальник бачив джерело на 160 тис. рядків
там, де фактично 88, — і брав хеш-з'єднання з повним скануванням журналу.

Згорталася вона лише тоді, коли `p_dims` — константа. У застосунку він не
константа ніколи: відбір збирається виразом
(`jsonb_build_object('nomenclature', …) || case when … end`). Тому наш стенд,
який кликав шар літералами, виграш бачив, а ваш живий шлях — ні.

Тепер тіло винесено в `app.acc_entries_impl(…, p_by_dims boolean)`, а
`app.acc_entries` розгортає його двічі — з `false` і з `true`. Літерал
вбудовується разом із тілом, мертва гілка згортається на плануванні, і кожна
половина дістає свій правильний план. Те саме в `acc_journal`.

**Сигнатури, форма результату й семантика `@>` не змінилися** — у ваших
викликах не міняється нічого. Рівність перевірена на 14 випадках стенда,
біт-у-біт.

Числа з нашого стенда (80 тис. проводок, чергування старої й нової функції в
одній сесії, медіана):

| Випадок | До | Після |
|---|---:|---:|
| живий виклик, один вимір (~90 рухів) | 45.6 мс | **10.6 мс** |
| живий виклик, два виміри | 58 мс | **30 мс** |
| повний журнал без відбору (контроль) | 335 мс | 349 мс |

Два виміри виграють менше, бо маловибірковий вимір (склад — половина журналу)
багато читає й індексом; контрольний рядок без відбору лишається в межах
розкиду.

**Дві нові функції в схемі — `acc_entries_impl` і `acc_journal_impl`.** Це
внутрішні тіла, кличте як і раніше `acc_entries` / `acc_journal`: у них
`p_by_dims` мусить збігатися з `p_dims`, інакше функція просто віддасть не те.

**Ціна рішення, названа чесно:** у плані тепер видно обидві половини (друга —
`never executed`), планування трохи дорожче, а сумарна оцінка рядків завищена на
мертву половину. На вибір шляху всередині це не впливає, і всі споживачі шару
над результатом лише агрегують. Якщо ви ЗʼЄДНУЄТЕ результат `acc_entries` з
чимось іще, а не агрегуєте, — скажіть, це єдине місце, де завищена оцінка може
вилізти.

---

## server 0.22.0 · tools 0.13.22 · skills 0.1.34 · create 0.7.33

Друк на регламентованих формах: поле по клітинках і вертикальний текст. Обидві
речі — властивості рендерера, і обійти їх із боку застосунку не виходило ніяк.

### Потребує дії

**Якщо у вас є СВОЯ копія редактора шаблонів друку** (`app/admin/print_template/`)
— вона перестане проходити `deno task check`, і це навмисно. У форматі
з'явилися два обов'язкові поля, і мовчки не показаний орган керування був би
гіршим за помилку типів: шаблон би зберігся без повороту, а причини не знав би
ніхто.

Дві правки в чужому коді на два рядки:

```ts
// app/admin/print_template/printTemplate.grid.ts → createCell()
    color: "",
    textOrientation: "0",          // ← додати

// app/admin/print_template/printTemplate.blocks.ts → createBlock("text") і createDefaultBlocks()
      format: "",
      textOrientation: "0",        // ← додати
```

Після цього збірка зелена, а шаблони друкуються як друкувалися. Щоб дістати самі
органи керування (поворот у властивостях блока й комірки, новий тип блока в
переліку), візьміть екран із репозиторію фреймворку — він там оновлений цілком.

**Шаблони мігрувати не треба.** Порожній `textOrientation` означає `"0"`, тож
усі наявні бланки чинні як є.

### Змінилося саме

**Блок `char-cells` — поле, розкладене ПО КЛІТИНКАХ.** Одна літера — одна рамка:
так друкуються ІПН і ЄДРПОУ, РНОКПП, дата складання, порядковий номер
декларації. Раніше це виражалося тільки рядком, і бланк формально не збігався із
затвердженим; таблиця не годилася — там рядки ДАНИХ, а тут одне значення,
розрізане на символи, посеред шапки.

```json
{ "type": "char-cells", "path": "doc.sellerTaxNumber", "count": "12",
  "placement": { "mode": "absolute", "xPercent": "22", "yPercent": "14", "widthPercent": "36", "heightPercent": "3" } }
```

`count` беріть із ЗАТВЕРДЖЕНОЇ форми, а не з довжини значення: порожні клітинки
на бланку лишаються порожніми, і це його нормальний вигляд.

**Ріже рендерер, форматує команда даних** — правило «рендерер не форматує»
винятку не дістало. `22.06.2026` у восьми клітинках дасть `2 2 . 0 6 . 2 0`:
крапки теж символи. Дату під клітинки віддавайте окремим полем
(`to_char(d, 'DDMMYYYY')`). Це найдорожча пастка тут, бо бланк при цьому
виглядає заповненим.

**`"textOrientation": "90"` — текст знизу вгору**, у блоці `text` і в комірці
таблиці. Заголовок авансового звіту вздовж лівого краю аркуша, заголовки вузьких
колонок інвентаризаційного опису, таблиці ЄСВ — усе це в затверджених формах
повернуте, і горизонтально його нема куди подіти. Кут рівно один: у 1590 макетах
джерела інших значень, крім 0 і 90, немає.

Дві речі, які з цього випливають і які видно лише на папері:

- у **повернутого блока висота рамки починає діяти на друк** — вона стає
  довжиною, на якій рядок переноситься (не задана — текст іде одним рядком на
  всю довжину);
- **повернута комірка не переноситься по словах** — довжина її напису стає
  висотою рядка таблиці.

**`@altera/server/print/render` — рендерер назовні.** Тепер бланк можна зібрати
в пробі, не заходячи в застосунок: `renderPrintPdf(templateItem, printData)`.
Раніше «чи влізе» рахувало ядро (`/print/metrics`, 0.17.1), а «як воно
виглядає» проба однаково діставала імпортом із `vendor/` і запуском із
`--no-config`. Шаблон, прочитаний з диска, спершу проженіть через
`normalizePrintTemplateSchema` — файл у `prints/` через нормалізацію не
проходив, і саме там береться `Cannot read properties of undefined`.

---

## server 0.21.3 · client 0.12.5 · tools 0.13.21 · skills 0.1.33 · create 0.7.32

### Потребує дії

**Опублікуйте SQL** — зміна знову цілком у базі:

```bash
deno task sql:assemble && deno task sql:publish
```

Міграція перебудує один індекс (`ix_journal_entry_analytic_value`); на великому
журналі це помітна пауза один раз, далі публікації його не чіпають.

### Змінилося саме

**Індекс аналітики дістав `journal_entry_id` і `side` у ключ.** Індексний вхід
за субконто (0.21.2) читає з індексу рівно ці дві колонки, а їх там не було —
тобто за кожним знайденим рухом ішов похід у купу. Тепер вхід index-only:
на журналі, що не вміщається в кеш, зникають тисячі випадкових читань диска на
виклик — а виклик стоїть у проведенні на кожен рядок документа. Префікс
`(dimension_code, value_id)` збережено: усі ваші запити по цьому індексу
працюють як працювали.

**Збірка більше не падає на ключах-коментарях у `manifest.json`.** Домовленість
`"//board": "чому це окрема в'ю"` знав генератор реєстру, але не сканер збірки
Vite: той діставав рядок замість опису в'ю, і `deno task build:front` помирав з
`The "path" argument must be of type string` — після зелених `sql:registry` і
`check`, не називаючи ні файлу, ні ключа. Тепер сканер пропускає такі ключі так
само, як реєстр.

**Нові гліфи `chevronLeft` / `chevronRight`** — крок до сусідньої колонки, та
сама пара, що `moveUp`/`moveDown` по вертикалі. Дошка (kanban) малює перенесення
картки ними, а не рукописним svg, який випав би з набору й `--icon-size`.

---

## server 0.21.2 · tools 0.13.20 · skills 0.1.32 · create 0.7.31

### Потребує дії

**Опублікуйте SQL — інакше нічого не зміниться:**

```bash
deno task sql:assemble && deno task sql:publish
```

Виправлення цілком усередині функцій ядра, а вони потрапляють у базу лише
публікацією. Пакет оновиться, а база лишиться зі старими функціями — і мовчки:
жодної помилки, просто колишня швидкодія.

**Заміряйте час проведення багаторядкового документа.** Ми заміряли прямі
виклики шару, а найдорожчий шлях у вас інший — добір собівартості кличе
`acc_entries` з plpgsql з обчисленим відбором, на кожен рядок. Правильність
такого виклику перевірена, а швидкодія саме його — ні (очікуємо, що виграш там
теж є, але це міркування, не замір). Якщо час проведення не впав — напишіть у
канал зворотного зв'язку, доробимо вхід під цей шлях.

### Змінилося саме

**Відбір за субконто входить у вибірку через індекс.** Доти `p_dims` фільтрував
уже побудовані рядки: `acc_entries` перебирала весь журнал організації,
розгортала на два боки, рахувала аналітику в jsonb на кожен рядок — і лише в
кінці застосовувала `@>`. Ціна виклику не залежала від вибірковості: залишок
номенклатури з 90 рухами коштував стільки ж, скільки з 6 000. Тепер, коли
`p_dims` непорожній, вхід іде з `ix_journal_entry_analytic_value` за заданими
парами (вимір, значення), а звідти — до проводок і шапок.

На стенді ~80 тис. проводок: холодна номенклатура ~760 → ~5.5 мс, ОСВ за місяць
з відбором ~1 010 → ~87 мс, залишки по партіях ~1 950 → ~325 мс, гаряча
номенклатура ~790 → ~120 мс. Виклики **без** відбору (звіт по всьому рахунку) не
змінилися — там скан законний.

**Правити нічого не треба.** Сигнатури, форма результату й семантика `@>` ті
самі: задані пари присутні, зайві субконто не заважають. Рівність перевірена
чотирнадцятьма випадками до і після — біт-у-біт, включно з крайовими
(неканонічний id `'007'`, нечислове значення, порожній `{}`). `acc_journal`
дістав те саме джерело; `acc_balance_turnover*`, `acc_balance` і `acc_turnover`
успадкували виграш через виклик `acc_entries`.

Запис зворотного зв'язку — `gaps/2026-08-16-acc-entries-dims-filter-not-indexed.md`;
це те виправлення, якого чекав партійний облік за ФІФО.

---

## tools 0.13.15 · skills 0.1.27 · create 0.7.26

### Потребує дії

**`app.<model>_unpost_records` тепер кличеться й на ПРОВЕДЕННІ — перегенеруйте
SQL документів** (`deno task sql:gen <family>/<model>`). У 0.13.12–0.13.14 гак
стояв лише в `unpost`, і цього мало: `doc_post_begin` повторному проведенню не
відмовляє, тож документ, проведений двічі поспіль без розпроведення, клав у чужу
таблицю ДРУГИЙ комплект рядків. У ПДВ-реєстрі чи в цінах це видно не при
проведенні, а в декларації.

Гак від цього не змінився — якщо він написаний як звичайний `delete ... where
document_id = ...`, обидва шляхи стають правильні самі собою: перепроведення
переписує начисто, як ядро вже робить із проводками.

---

## tools 0.13.19 · skills 0.1.31 · create 0.7.30

**Перевірка «вміст проти реєстру» чесно пропускається на мілкій історії.** У
клоні глибиною 1 граничний коміт диффиться проти порожнього дерева, тож
`git log -- <пакет>` знаходив його для БУДЬ-ЯКОГО каталогу — і перевірка
звинувачувала коміт, який пакета не чіпав. Тепер shallow-репозиторій дає
«пропущено», а checkout у publish-workflow бере повну історію з тегами — там
вердикт потрібен справжній.

---

## server 0.21.1 · tools 0.13.18 · skills 0.1.30 · create 0.7.29

### Змінилося саме

**Розріз шару обчислень — за КІЛЬКОМА вимірами одразу:**
`acc_balance_turnover_by_dims(org, from, to, accounts, dims,
array['warehouse','nomenclature'])` — «скільки чого й ДЕ» одним проходом, з
кількістю поруч із грішми. Значення вимірів їдуть колонкою `dims jsonb`,
ключованою кодом виміру (`dims->'warehouse'->>'presentation'`) — за ім'ям, а не
за позицією. Рух, у якого виміру немає, не зникає — його ключа просто немає в
об'єкті, тож сума розрізу так само дорівнює сальдо рахунку.

**`_by_dim` нікуди не дівається** — стала плоскою обгорткою над `_by_dims`:
форма відповіді та сама, ваші звіти на ній не міняються, а методологія тепер
сказана один раз. Це закриває другу половину запису «шар не віддає кількості й
уміє лише один розріз»: обхід `app.inventory_turnover` можна знімати цілком.

---

## tools 0.13.17 · skills 0.1.29 · create 0.7.28

**Перевірка «вміст проти реєстру» більше не червоніє на здоровому релізі.**
Точкою відліку був коміт, який підняв версію, а реліз законно їде поїздом
(бамп першим комітом, пуш усього разом) — публікується head, і «коміти після
бампа» лежали в реєстрі, а перевірка вважала їх неопублікованими, причому лише
ПІСЛЯ публікації. Тепер відлік — тег `jsr/<пакет>@<версія>`, який ставить
publish-workflow; для версій без тега — час публікації з реєстру.

---

## server 0.21.0 · tools 0.13.16 · skills 0.1.28 · create 0.7.27

### Потребує дії

**`app.acc_journal` віддає кількість і валюту по боках** — замість `quantity`,
`currency_code`, `currency_amount` тепер `quantity_debit`/`quantity_credit`,
`currency_code_debit`+`currency_amount_debit` /
`currency_code_credit`+`currency_amount_credit`. Звіт, який читає журнал проводок
(рухи документа), мусить перейти на нові імена; еталон
`app/report/document_movements` уже переведений — колонка малюється, лише коли її
бік є в даних. Функції сальдо-й-оборотів форми не міняли; `acc_entries` теж ні —
але його `currency_debit`/`currency_credit` тепер заповнюються ЛИШЕ на боці, що
веде валюту: доти сума діставалася обом бокам без огляду на `is_currency`, і
картка гривневого рахунку в проводці «Дт 312 Кт 311» показувала валютну суму.

**Позначка на видалення документа = скасування проведення — перегенеруйте SQL
документів.** Позначений документ більше не тримає проводок: `delete`
проведеного спершу знімає проведення (разом із вашим `_unpost_records`), потім
ставить позначку — тож заборона закритого періоду діє й на видалення
проведеного. **Зняття позначки проведення не повертає**: документ стає
непроведеним, провести треба заново, свідомо. Доти «правду» про позначений
документ виражав фільтр читання — проводки лежали, звіти їх не бачили, а
«Відновити» мовчки повертало рухи в баланс. Наявні позначені проведені документи
приводить до інваріанта міграція при `sql:publish` (видимих чисел вона не міняє
— їхні рухи й так були виключені з кожного звіту); ті, що прослизнуть повз неї,
доліковує `undelete`.

**Згенерований `save` відмовляє на проведеному документі — перегенеруйте SQL
документів.** Форма й так відкриває проведений документ лише на читання, але
підказка клієнта — не захист: через API чи агентом реквізити переписувалися без
жодної відмови, а рухи в регістрі лишалися від старих даних. Розходження, якого
не видно, доки хтось не звірить документ зі звітом. Тепер відмовляє сервер;
розпровів — редагуй — проведи, як і малювалося.

**Міграція `app.journal_entry` при `sql:publish`**: `quantity` розкладається на
`quantity_debit`/`quantity_credit`, а пара `currency_id`+`currency_amount` — на
бічні четвірки, обидві за ознакою рахунку (значення дістає бік, що веде вимір —
те саме правило, яким їх доти роздавав `acc_entries`); старі колонки зникають.
Прямий запит у регістр повз шар, якщо він у вас є, перестане компілюватися — це
навмисно голосно, бо мовчки він почав би брехати.

### Змінилося саме

**Складна проводка виражається регістром: кількість зберігається ПО БОКАХ.**
Комплектація — 2 комплекти з 6 корпусів і 2 матриць — це тепер два звичайні
рядки однієї кореспонденції:

```sql
perform app.doc_entry_add(doc_id, 1, '281', '281', 600.00, null, 'корпуси',
  dims_kit, dims_case, null, null, '{"debit": 2, "credit": 6}'::jsonb);
perform app.doc_entry_add(doc_id, 2, '281', '281', 600.02, null, 'матриці',
  dims_kit, dims_matrix, null, null, '{"credit": 2}'::jsonb);
```

Кількість — небалансовий вимір, як у джерелі цієї моделі: сума одна на проводку
(це і є подвійний запис), а кількості боків незалежні, бо шість корпусів дають
два комплекти — нерівність тут зміст операції. Кількість головного боку пишеться
один раз, на одному з рядків; підсумки по рахунку складаються самі, а в розрізі
номенклатури комплект дістає +2, кожен компонент іде в нуль.

**Новий параметр — вираження наміру.** `p_quantity` працює як працював: одне
число на обидва кількісні боки, і забути його так само не можна. `p_quantities`
(`{"debit": 2, "credit": 6}`) задає боки окремо, і перелік тут і є захистом:
кількісний бік, відсутній в об'єкті, лишається порожнім ЗАКОННО — голова
складної проводки живе на сусідньому рядку. Порожній об'єкт `{}` — переоцінка:
сума без кількості на кількісному рахунку, що доти не виражалася нічим.

**Механізму «групи» проводок немає навмисно.** Сховище групи однаково тримало б
кількість голови на одному рядку з N, а сверяти при «закритті групи» нема чого —
сума головної частини дорівнює сумі частин за побудовою. Якщо колись друкованій
формі знадобиться показувати складну проводку деревом, ознака групи додається
аддитивно; протокол із трьох функцій — ні.

**Валюта — по боках тим самим махом, і в парі з сумою.** Конвертація
«Дт 312 USD Кт 314 EUR» — дві різні валюти в одній проводці:

```sql
perform app.doc_entry_add(doc_id, 1, '312', '314', 4100.00, null, 'конвертація',
  dims_312, dims_314, null, null, null,
  '{"debit": {"id": 3, "amount": 100.00}, "credit": {"id": 4, "amount": 92.00}}'::jsonb);
```

Доти вона виражалася лише через рахунки «в дорозі» (333/334) — той обхід
методологічно законний і лишається законним, але був ЄДИНИМ способом, а став
вибором. Legacy-пара `p_currency_id`+`p_currency_amount` діє як діяла: одна
валюта й сума на кожен валютний бік (переказ усередині валюти). Перелічений у
`p_currencies` бік мусить бути повним — `id` без `amount` це помилка, а не намір;
ЦІЛА порожня пара (`"debit": null`) — законний намір «без валютної суми», як у
кількості.

**Чотири дірки формування проводок закриті заразом:**

- **сума, що округлюється в нуль** (`0.004`), проходила перевірку «не нуль» і
  лягала в регістр нулем — numeric(18,2) округлює мовчки; тепер нуль
  перевіряється після округлення. Від'ємна сума лишається законною навмисно —
  це сторно, коригування на тій самій кореспонденції;
- **подвійне задання виміру** (`p_quantity` разом із `p_quantities`, або
  legacy-валюта разом із `p_currencies`) відмовляє замість мовчазної переваги
  одного зі способів: котрий мав на увазі документ — звідси не видно;
- **паралельне проведення того самого документа** серіалізується
  (`for update` у `doc_post_begin`): доти двоє встигали знести старі проводки й
  писати нові, і другий падав на унікальності номера рядка — помилкою за два
  кроки від причини;
- **`save` на проведеному документі** — див. «Потребує дії».

---

## tools 0.13.14 · skills 0.1.26 · create 0.7.25

**`scaffold:verify` тепер відмовляє, коли пакет змінився після свого релізу.**
Вас це стосується опосередковано — але саме цей дефект три рази поспіль доїжджав
до вас у вигляді застосунку, який не збирається: у реєстрі лежить одна версія, у
репозиторії під тим самим номером інша, і бачив це лише крок ПІСЛЯ публікації.
Тепер перевірка стоїть до неї.

---

## client 0.12.4 · skills 0.1.25 · create 0.7.24

**Три експорти клієнта нарешті опубліковані** — `tabs/tab-url.ts`,
`ui-kit/components/ui-dialog.ts`, `ui-kit/components/ui-remark.ts`. Вони були
додані до export-мапи ще 13 серпня разом з екраном зауважень, але версію клієнта
тоді не підняли, тож в опублікованому `0.12.3` їх немає. Наслідок бачив кожен, хто
створював застосунок scaffold-ом: `deno check` падав на
`Unknown export './tabs/tab-url.ts' for '@altera/client@0.12.3'`. Якщо ви ставили
свіжий застосунок і впиралися в це — досить оновити пін клієнта.

---

## server 0.20.0 · tools 0.13.13 · skills 0.1.24 · create 0.7.23

Зміна тут одна — кількість у шарі обчислень. У `tools` і `create` не змінилося
нічого, крім пінів: попередні їхні номери вже пішли в реєстр із версією сервера
`^0.19.0`, а тягнути в них сервер 0.20.0 заднім числом ніде.

### Потребує дії

**`acc_entries` більше не приписує кількість тому боку, який її не веде.** У
регістрі колонка кількості одна, і заповнюється вона, коли кількісний ХОЧ ОДИН
бік проводки, — тож у «Дт 281 Кт 631 на 10 шт» другій стороні діставалися ті самі
10 штук, хоча рахунок 631 кількості не веде взагалі. Тепер `quantity_debit` і
`quantity_credit` порожні там, де в рахунку немає `is_quantitative`.

Помітно це в двох місцях, і обидва — на краще: у картці рахунку 631 зникне
колонка кількості з правдоподібними числами, а власний запит по `acc_entries`,
який ці числа складав, почне давати порожньо замість суми. Якщо ви на них
спиралися — спиралися на брак.

**`acc_balance_turnover` і `acc_balance_turnover_by_dim` віддають чотири нові
колонки.** Виклик `select * from` дістане їх у кінці; іменовані вибірки не
зачеплені. Якщо ваш звіт складає рядок позиційно — перевірте.

### Змінилося саме

**Шар обчислень тепер віддає КІЛЬКІСТЬ поруч із грішми** —
`opening_quantity`, `quantity_debit`, `quantity_credit`, `closing_quantity` в
обох функціях сальдо-й-оборотів. Доти кількість жила тільки в `acc_entries`,
тобто **до** будь-якої арифметики: оборотка по складу мусила заново писати поділ
на вхідне сальдо й оборот, згортку по ключу й складання кінцевого залишку — сорок
рядків SQL, які зобов'язані збігатися з ядром до копійки й перестають про це
знати, щойно ядро змінить правило.

Порожньо там, де рахунок кількості не веде, і нуль там, де веде, але руху не
було. Різниця не косметична: нуль на негрошовому рахунку читається як «поміряли
й вийшло нічого», а тут не міряли зовсім.

Заразом рядок із рухом **лише в штуках** (безоплатна передача, перекомплектація)
перестав зникати: умова «рахунки без руху не повертаємо» дивилася тільки на гроші.

**Розріз усе ще один** — `..._by_dim` бере один `p_dimension_code`, тож
«скільки чого й ДЕ» (склад × номенклатура) через шар поки не питається. Це
наступна робота, і вона міняє форму результату, а не додає колонки.

---

## tools 0.13.12 · skills 0.1.23 · create 0.7.22

### Потребує дії

**Ключ-коментар у манифесті більше не валить генерацію — але перевірте, що він у
вас не став командою.** `"//picker": "…"` у блоці `views` падав із `Path must be
a string, received "undefined"`, і це було видно. Гірше було там, де видно не
було: той самий коментар у `commands.sql` чи `commands.access` тихо ставав
записом реєстру з іменем «//…». У цьому репозиторії таких набралося чотири. Тепер
ключі з `//` прибираються при читанні манифеста — на всіх рівнях. Перегенеруйте
(`deno task sql:registry`) і подивіться в дифф `model-registry.generated.ts`.

Заразом генератор більше не пише НІЧОГО, доки не зібрав усе: помилка в одному
манифесті лишала застосунок із частково оновленою генерацією — реєстр,
ts-commands і agent-routes уже на диску, view-manifest ні, — і виглядало це як
зламаний застосунок, а не як помилка в манифесті.

**`sql:gen` тепер відмовляє на ссылочному фільтрі звіту, чий підпис розходиться з
поданням цілі.** Якщо генерація впала з таким текстом — це не регрес, а мовчазна
помилка на екрані, яку нарешті видно:

```
stock_balance: фільтр «warehouse» оголошений як { id, name }, а подання моделі
warehouse — колонка «code» (перше поле з x-lookup). Додайте display: "name" в
x-ref або оголосіть фільтр як { id, code }
```

Эхо обгортки бере ключ підпису з першого поля цілі з `x-lookup` — у більшості
довідників це `name`, а в складі, одиниці виміру й банківському рахунку `code`, —
і приходить першим у `v_filters || …`, тобто ЗАТИРАЄ підпис, покладений формою.
Відбір діє, числа правильні, а пікер у панелі порожній; схема при цьому валідна,
SQL зелений, і причину йдуть шукати у форму й у пікер. Лікується одним словом —
`display` у `x-ref`. Оголошення без підпису (`{ id }`) проходить як і раніше.

### Змінилося саме

**У розпроведення з'явилася пара до `_post_entries` — необов'язковий
`app.<model>_unpost_records(user_id, document_id)`.** `doc_unpost` чистить
`app.journal_entry` і більше нічого: про рядок, покладений документом у ЧУЖУ
таблицю — періодичний реєстр цін, склад, ПДВ, — ядро не знає нічого. Без цього
ціни розпровединого документа далі діяли в накладних, а екран показував документ
непроведеним; дописати умову «лише проведений реєстратор» у читання чужого
реєстру теж нікуди, коли воно генерується (`periodic`).

```sql
create function app.price_setting_unpost_records(user_id bigint, document_id bigint)
returns void language plpgsql as $$
begin
  delete from app.nomenclature_price
  where document_id = price_setting_unpost_records.document_id;
end $$;
```

Вмикається СТВОРЕННЯМ функції — так само, як `app.doc_before_write`, — тож
документи, у яких рухи лише проводками, не міняються зовсім. Функція з тим самим
іменем і **іншим підписом** валить розпроведення: мовчазний пропуск тут гірший за
відмову, бо застосунок був би певен, що рухи зняті. Обхід — власний тригер на
`app.document` — можна знімати, і варто: на ту саму таблицю вішає тригери й ядро,
а порядок їхнього спрацювання ніде не записаний.

**Скіл `model-print-form` тепер каже, ЧИМ секції таблиці необов'язкові.** Він і
раніше обіцяв «Sections are optional», а в коді плановика видно зворотне — тож
шаблон без `footer` виглядав як міна, і його підпирали порожнім масивом у файлі.
Насправді відсутні секції складає `normalizePrintTemplateSchema`, через який
проходять обидва входи друку, тож збережений шаблон до плановика напівописаним не
доходить. Доходить той, який **читають самі** — вимірювач розкладки, міграція,
скрипт над `prints/*.template.json`; це єдиний спосіб зустріти `Cannot read
properties of undefined (reading 'filter')`, і лікується він викликом того самого
нормалізатора з `@altera/server/print`, а не правкою шаблону.

---

## tools 0.13.11 · skills 0.1.22 · create 0.7.21

### Змінилося саме

**`x-ref` на модель-ДОКУМЕНТ тепер працює.** Доти послатися на документ у схемі
моделі не можна було ні на який: генератор будував join за колонкою `id`, а в
таблиці документа її немає взагалі — первинний ключ там `document_id`, і саме
цю розкладку («шапка в ядрі, реквізити в таблиці моделі») задає сам фреймворк.
Публікація падала на `column r_shipment.id does not exist`, тобто за два кроки
від причини.

Оголошується як будь-яка інша ссылка — тип цілі генератор бере з її
`manifest.json`:

```ts
shipmentId: Type.Optional(Type.Union([Type.String(), Type.Null()], {
  title: "Документ відвантаження", "x-db-type": "bigint",
  "x-ref": { model: "goods_sale", as: "shipment" },
})),
```

**Подання за умовчанням — `presentation` шапки з падінням на номер**, тож у формі
поле це `<ui-picker display-field="presentation">`. Це та сама колонка, якою
підписаний рядок у журналі документів, і заповнює її **необов'язковий** хук
`app.<model>_denormalize` — документ, який його не має, показує номер, а не
порожній рядок. «№1 від 22.07.2026» генератор не складає навмисно: слово «від» —
це текст мовою, а сервер тексту не перекладає. Хочете саме такий підпис — складіть
його у своєму `_denormalize`.

`display` вибирає колонку явно й працює по обидва боки: `"number"` і `"doc_date"`
— шапка, будь-яка інша — власний реквізит документа. Ключ вкладеного об'єкта —
camelCase від колонки (`doc_date` → `docDate`). `searchable` дозволений лише по
текстовому поданню (`number`, `presentation`, `description`) — `ilike` по даті це
помилка типу, і без відмови на генерації вона вилізла б на першому наборі символів
у полі пошуку.

Те саме діє у **ссылочному фільтрі звіту**: інакше одна анотація працювала б у
формі й падала у фільтрі.

Обхід — «звичайна колонка `bigint` плюс добір номера окремим запитом у формі» —
можна прибирати: він коштує зайвого запиту на кожне відкриття документа, а ссылка
при ньому невидима для списку й для агента.

---

## tools 0.13.10 · skills 0.1.21 · create 0.7.20

### Змінилося саме

**Токен доступу можна видати з консолі** — `jsr:@altera/tools/issue-access-token`.
Ендпойнт `/api/auth/tokens` видає токен лише по сесії (агент, який тримає токен,
не має карбувати собі нові), тож поза браузером узяти його не було звідки: ані
підключити MCP-обгортку, ані написати скрипт, ані завести інтеграцію на сервері
без інтерфейсу.

Умовчання — **тільки читання**, і це не перестраховка: токен успадковує ВСІ права
свого користувача, а видають його звичайно для роботи, де запис не потрібен.

**Скіл `model-feature-architecture` тепер вимагає заповнювати `agent.aliases`.**
Зовнішній агент вибирає модель із каталогу, де є технічне ім'я, назва з локалей і
синоніми; на рішенні з сотні моделей саме синоніми й вирішують, чи знайде він
потрібну — людина просить «оборотку», а не `turnover_balance`. Правила короткі:
слова з мови користувачів (обома мовами, якими з базою розмовляють), унікальні на
весь застосунок (двозначний синонім не годиться жодній моделі), 3–6 на модель,
`priority: 10` для щоденних. Заповнювати при створенні моделі: дописувати
синоніми до готового рішення — окрема робота, яку ніхто не планує.

### Тільки для нових застосунків

У шаблон додано задачу — наявні застосунки дописують рядок самі:

```json
"token": "deno run -A --env-file jsr:@altera/tools@^0.13.10/issue-access-token"
```

```bash
deno task token claude-mcp          # тільки читання
deno task token нічна-звірка --write
```

---

## server 0.19.0 · tools 0.13.9 · create 0.7.19

### Потребує дії

**Вхід агента перейменовано: `POST /api/agent/chat` → `POST /api/agent/call`.**
Старої адреси немає — 404. Тіло й відповідь ті самі (`{model, command,
payload}`), змінилося ім'я: чату там ніколи не було, а пара з
`GET /api/agent/tools` тепер читається як у MCP — перелік інструментів і виклик
інструмента.

**Перегенеруй опис інструментів** — `deno task sql:registry`. Без цього агент не
побачить звітів (див. нижче): `agent-tools.generated.ts` — закомічений вихідник,
а не продукт збірки.

**`GET /api/agent/tools` без параметрів віддає тепер КАТАЛОГ, а не схеми.**

```
GET /api/agent/tools                          → [{model, type, route, aliases, priority, commands}]
GET /api/agent/tools?model=bank,invoice       → [{model, command, input}] — схеми названих моделей
GET /api/agent/tools?model=bank&command=save  → одна команда
```

Хто читав плаский перелік `{model, command, input}`, має або спитати схеми
поіменно, або пройти каталогом і добрати потрібне. Кілька моделей — через кому,
одним запитом. Названа модель, якої немає, — відмова, а не порожній список, і в
списку відмовляє будь-яка невідома: віддати решту мовчки означало б дати агенту
вважати, що він отримав усе, що просив.

**`uiActions` замінені на `route`.** Дії для екрана (`openRoute`, `showToast`)
наказували щось зробити оболонці, яка їх не слухала, і будувалися на кожен
виклик; до того ж `openRoute` віддавав `/catalog/bank/edit?id=5`, тоді як
вкладку відкриває `/catalog/bank/edit/5`. Тепер у результаті лежить готове
посилання на вкладку:

```json
{ "ok": true, "result": { "model": "invoice", "id": "42",
  "route": "/document/invoice/edit/42" }, "messages": [] }
```

Відповідь тепер `{ok, result, messages}`. Посилання є лише на успіху; відборів
воно не несе, тож звіт відкриється порожнім.

### Змінилося саме

**Агент бачить звіти.** Оборотно-сальдова відомість, картка рахунку, рухи
документа — команда `index` із схемою фільтрів. Доти агент умів створити й
провести документ, але не міг подивитися оборотку, тобто мав рівно протилежний
до потрібного набір: звірка починається з читання регістру, а не з запису.

Право звітної команди береться **з оголошеного** (`commands.access` у
манифесті), а не виводиться з імені. Вивід знає лише вісім стандартних дій —
через нього нестандартні команди були невидимі для агента, хоч і виконувані.

**Чому перелік розділили.** Двадцять моделей дають 65 інструментів і 26 КБ;
повне рішення — сотню моделей, тобто 400–600 інструментів і близько 200 КБ в
одній відповіді, причому нелінійно (`save` документа з табличною частиною важить
у рази більше за `lookup` довідника). Каталог від цього не росте так само:
заміряно 127 байтів на модель, тобто ~13 КБ на сотню, — його можна тримати
цілком, а схему брати тоді, коли справді збираєшся викликати.

**У каталозі з'явилися назви й синоніми.** `titles` — назва моделі мовами
застосунку (`{"uk": "Банки", "en": "Banks"}`), взята з `titleKey` списку при
генерації; `aliases` і `priority` з `manifest.agent` генерувалися в маршрути, але
агенту не показувалися взагалі. Технічне ім'я `nomenclature` не каже нічого тому,
хто цієї бази не бачив, — а на сотні моделей вибирати доводиться саме за назвою.
Назви беруться зі **зібраних** локалей: без `deno task locales:build` їх просто
не буде.

**Ім'я моделі має бути унікальним на весь застосунок — тепер це перевіряється.**
`sql:registry` відмовляє й називає обидва манифести. Доти другий `model` із тим
самим іменем мовчки перемагав у реєстрі, а зіткнення вилазило аж при публікації
SQL. Префікса родини в імені немає й не буде: під ним лежать SQL-функції однієї
схеми, рядки прав і аудит, тож ім'я мусить пережити переїзд моделі з довідників
у регістри — родина їде окремим полем (`type`, `route`).

**Білий список диспетчера — це той самий перелік інструментів.** Раніше
`/api/agent/chat` мав власний набір із п'яти імен, і саме через нього звіт не
викликався б: перелік показував би `index`, а диспетчер відмовляв. Тепер
дозволене — рівно те, що агент прочитав у `/api/agent/tools`, тобто зібране з
манифестів (`agent.allow`, `agent.allowCommands`). Двох списків, які тихо
розходяться, більше немає.

---

## server 0.18.1 · tools 0.13.8 · create 0.7.18

### Змінилося саме

**`DEV_AUTH_BYPASS` більше не підміняє невдалий токен.** Запит із
`Authorization: Bearer`, який не підійшов — зіпсований токен, відкликаний,
протермінований, власник деактивований, — дістає 401 навіть при ввімкненому
обході.

Доти такий запит тихо ставав запитом користувача за замовчуванням: зламаний
токен виглядав робочим, а відкликаний — чинним, і побачити це можна було лише на
чужій базі, де обходу немає. Тобто режим, у якому розробник працює щодня, був
єдиним, де перевірити токен нічим.

Обхід лишається тим, чим має бути: підстановкою для запиту **без** облікових
даних. Дев-цикл не змінюється — браузер `Bearer` не шле, `deno task api --user`
працює як працював. Cookie сюди свідомо не входить: протермінована сесія в
розробці має вести до звичайної роботи, а не до 401 на кожен клік.

---

## server 0.18.0 · tools 0.13.7 · create 0.7.17

### Потребує дії

**Прибери `OPENAI_*` зі свого `.env`** — `OPENAI_API_KEY`, `OPENAI_MODEL`,
`OPENAI_ROUTER_MODEL` більше не читаються нізвідки. Лишені рядки нічого не
ламають, але обіцяють можливість, якої немає.

**Якщо передавав `agent:` у `bootstrap()`** — прибери: блока в `ServerOptions`
більше немає, і збірка впаде на типах. Це стосується лише ключа OpenAI;
`agentRoutes` і `agentTools` лишаються на місці й нікуди не діваються.

```ts
const application = await bootstrap({
  ...configFromEnv(),
  agent: { openAiApiKey: "…" },   // ← прибрати
  agentRoutes: agentModelRoutes,  // ← лишається
  agentTools: agentToolSchemas,   // ← лишається
});
```

Разом із блоком зник експортований тип `AgentConfig` і поле `agent` у результаті
`configFromEnv()`.

### Змінилося саме

**LLM-агента в сервері більше немає.** Видалено `agent-llm.service.ts` (клієнт
OpenAI Responses API з власним роутером моделей і tool-циклом) і ендпойнт
`POST /api/agent/llm-chat` — тепер це 404.

Причина не в тому, що LLM не потрібен, а в тому, ЧИЙ він. Своєї «розумної»
частини ми не пишемо: агент, здатний тримати контекст і вирішувати, — чужий
продукт, і догнати його неможливо. Наша роль — бути добрим постачальником
інструментів: `GET /api/agent/tools` каже, що можна робити, `POST
/api/agent/chat` це виконує, права й аудит рахуються на кожній команді.
Вбудований клієнт OpenAI до цієї ролі не належав: його не кликав ніхто, а їхав
він у кожен опублікований пакет — разом із ключем у конфігурації.

Помічник усередині застосунку («проведи цей рахунок» у чаті на екрані) цим не
скасовується, але це інша задача й інше рішення: вона вимагає LLM на СЕРВЕРІ,
тобто рівно того, що тут прибрано.

---

## server 0.17.6 · tools 0.13.6 · create 0.7.16

### Змінилося саме

**Запобіжники запису для агента.** Обидва діють ЛИШЕ на виклики персональним
токеном — на застосунок у браузері не впливає нічого.

**Токен «тільки читання»** (`isReadOnly` при видачі) відмовляє будь-якій
команді, що змінює дані, незалежно від прав користувача. Це не заміна правам:
право відмовляє тому, кому не можна взагалі, а прапорець — тому, кому можна,
але не на цій задачі. Такий токен ще й **не бачить інструментів запису** в
`/api/agent/tools`: вони йому однаково відмовлять, а показані виглядають як
доступні.

**Зміна стану вимагає підтвердження.** `delete`, `undelete`, `post`, `unpost`
проходять лише з `"confirm": true` у payload:

```json
{ "id": "42", "confirm": true }
```

Людини це не стосується — вона підтвердила натисканням кнопки. Агент мусить
сказати це словом: провести документ «щоб подивитися» не можна.

Вимога стоїть **у схемі інструмента**, а не лише в перевірці: `confirm`
з'являється в `required` із `const: true`. Інакше агент дізнавався б про неї
відмовою — тобто витрачав крок і міг би вважати відмову випадковою.

Перевірка живе в `ModelRuntimeService.execute`, у тій самій точці, де рахується
право, тож обійти її іншим входом не виходить.

---

## server 0.17.5 · tools 0.13.5 · create 0.7.15

### Потребує дії

**Передай нове поле в `bootstrap()`** — інакше перелік інструментів агента
порожній:

```ts
// app/server.ts
import { agentToolSchemas } from "./_generated/agent-tools.generated.ts";

const application = await bootstrap({
  ...
  agentRoutes: agentModelRoutes,
  agentTools: agentToolSchemas,   // ← додати
});
```

Файл з'являється сам при `deno task sql:registry` — окремої задачі й нового
аргументу немає навмисно: рядки задач у застосунках пінені за версією
інструмента, і новий позиційний аргумент означав би, що файл мовчки не
з'явиться в того, хто його не дописав.

### Змінилося саме

**`GET /api/agent/tools` — що агент уміє робити в цій базі.** Віддає
`{model, command, input}`, де `input` це JSON Schema payload-а, зібрана з
TypeBox-схем моделей. Далі агент кличе звичайні
`POST /api/model/:model/:command` — окремого API для нього немає й не треба.

Доти опис інструмента виглядав так:

```ts
item: { type: "object", description: "Поля головного запису…" }
```

Тобто складу реквізитів, типів, обов'язковості й того, куди веде посилання,
агент не бачив. Тепер бачить — разом із `title` полів і `x-ref`.

Три речі, які варто знати:

- **у документа шапка підмішується** з `DocumentHeaderSchema`: `ItemSchema`
  несе лише власні реквізити, а дата, організація й номер живуть у
  `app.document`. Без цього агент не побачив би дати;
- **екранні анотації не йдуть у контекст** (`x-form`, `x-list`, `x-lookup`,
  `x-search`, `x-db-type`, `x-filter`) — вони коштують місця й нічого агенту не
  кажуть. `x-ref` лишається;
- **перелік звужується правами користувача**. Це не захист (він нижче, у
  рантаймі, і fail-closed), а чесність опису: інакше агент витрачав би крок на
  команду, яка йому відмовить.

Описи будуються лише для типів зі стандартним CRUD (`catalog`, `document`,
`register`). Звіт має свій `index`, admin-екран — рукописний набір; показати їм
п'ятірку означало б дати агенту інструменти, що повертають 501.

---

## server 0.17.4 · tools 0.13.4 · create 0.7.14

### Потребує дії

**Виконай `deno task sql:publish`** — з'явилася таблиця `app.access_token` і три
функції ядра. Без цього нове нічого не зламає, але й не запрацює.

### Змінилося саме

**Персональні токени доступу — облікові дані для МАШИНИ.** Зовнішній агент
(Claude через MCP, скрипт, інтеграція) не відкриває браузер і не вводить
пароль, а сесія без цього не народжується.

```
GET  /api/auth/tokens          свої токени: назва, стан, коли востаннє вжитий
POST /api/auth/tokens          видати; значення повертається ОДИН раз
POST /api/auth/tokens/revoke   відкликати свій
```

Далі агент ходить звичайним `Authorization: Bearer <токен>` у будь-яку команду
моделі — окремого API для нього немає й не треба.

Три властивості, які варто знати, бо вони навмисні:

- **Токен не несе прав.** Він лише називає користувача; `access_can`
  рахується як завжди. Агент не може більше за людину, від імені якої діє, і
  другого механізму прав не з'являється.
- **Видати токен можна лише З БРАУЗЕРА** (усі три дії вимагають сесії). Агент,
  що тримає токен, не карбує собі нові — інакше вкрадений токен відтворював би
  себе нескінченно.
- **Зберігається хеш**, значення показується один раз. Вкрадений дамп бази
  доступу не дає.

Це перший крок плану [`docs/external-agent-plan.md`](docs/external-agent-plan.md).
Наступний — опис інструментів зі схем моделей: сьогодні агент бачить `item` як
`{type: "object"}`, тобто не знає ні складу полів, ні типів.

---

## client 0.12.3 · skills 0.1.20 · create 0.7.13

### Потребує дії

**«Скинути» в панелі відборів повертає тепер до УМОВЧАНЬ екрана, а не до
порожнечі.** Якщо ти обходив це перекриттям `resetFilters()` або ховав кнопку
правилом CSS — прибирай і те, і те.

```ts
protected override defaultFilters() {
  return { ...super.defaultFilters(), dateFrom: periodStart(), dateTo: periodEnd() };
}
```

Журнал документів відкривається звуженим — на поточну організацію (це робить
основа) і на період із налаштувань користувача. Це не прикраса: журнал за ВЕСЬ
час — найдорожчий запит екрана, а без організації він показує суміш двох
обліків. Доти виходили дві правильні поведінки, що суперечать одна одній:
основа сіяла відбір навмисно, а кнопка поруч його знімала.

`super.defaultFilters()` кликати обов'язково — інакше мовчки зникне умовчання
основи. Кнопка гасне, коли екран стоїть рівно на умовчаннях, а не коли відборів
немає взагалі. Прибрати її зовсім — `protected override filterReset = false`;
потрібно рідко, відколи скидання перестало бути небезпечним.

**`<textarea>` більше не сплюснутий у 24px — `rows` знову керує висотою.**
Прибери обхід, якщо писав:

```css
/* більше не потрібно */
.textarea { height: auto; }
```

Тема задавала однакову фіксовану висоту всім трьом контролам поля. Для
`<input>` і `<select>` це правильно, для `<textarea>` — ні: `height` мовчки
скасовував `rows`, і багаторядкове поле (призначення платежу, зміст послуги,
підстава, адреса) зробити було НЕ МОЖНА. Розмітка при цьому виглядала
правильною, тож видно це лише на екрані — і читалося як «контрол чомусь
однорядковий», а не як «тема перебиває rows».

`min-height: 24px` лишився спільним: порожня textarea не стає нижчою за сусідній
інпут. Якщо в тебе є `<textarea>` без явного `rows`, він тепер займе два рядки
(типове значення атрибута) — постав `rows="1"`, якщо потрібен саме один.

---

## server 0.17.3 · tools 0.13.3 · skills 0.1.19 · create 0.7.12

### Змінилося саме

**Знак гривні `₴` тепер друкується — з другої гарнітури.** Раніше він виходив
порожньою рамкою: U+20B4 немає ні у WinAnsi, ні в жодному зі 162 файлів
`@fontsource/roboto` (перевірили скануванням усі). У пакет доданий один субсет
**PT Sans** — рівно заради цього гліфа; з трьох гарнітур, у яких він знайшовся
(Inter, Noto Sans, PT Sans), узята найдешевша: +21 КБ на обидва накреслення.

Основна гарнітура бланка лишається Roboto, тож **жоден наявний бланк не міняє
вигляду**, а ширини колонок не треба переперевіряти. Ціна чесна: `₴` намальований
іншою гарнітурою, ніж цифри поруч — для знака валюти це непомітно. Жодна
українська літера в другу гарнітуру не провалюється: Roboto покриває і `Ґґ`, і
`ЄІЇєії`.

Якщо ти вже поставив `server@0.17.2` — лапки в ньому полагоджені, а гривня ще
ні: вона не встигла в ту версію. Онови до 0.17.3.

---

## server 0.17.2 · tools 0.13.2 · skills 0.1.18 · create 0.7.11

### Потребує дії

**Лапки-ялинки в бланках друкувалися сміттям. Передрукуй усе, що вже віддавав
замовникам.**

`ТОВ «Демо»`, `АТ «Ощадбанк»` — лапки має назва будь-якої української юрособи,
тож бланк без кракозябр був тільки той, у якому не згадана жодна сторона.
Помітив це замовник, а не проби: команда даних віддає правильний рядок, шаблон
правильний, PDF будується.

Шрифт вибирався за межею `код <= 0x7F` — усе вище йшло у вбудований субсет
Roboto, а `«` це U+00AB, і в кирилічному субсеті його немає. Іронія в тому, що
Helvetica ці символи має: питання просто ставилося не те — не «чи це ASCII», а
**«чи є гліф»**. Тепер саме так, і разом із лапками полагодилися `— – ’ … “ ” „`.

Робити нічого не треба — крім як подивитися на вже надруковане.

**`fonts.generated.ts` змінив форму.** Дві константи з base64 замінені списком
субсетів із діапазонами покриття:

```ts
export const PRINT_FONT_SUBSETS_REGULAR: readonly PrintFontSubset[]
export const PRINT_FONT_SUBSETS_BOLD: readonly PrintFontSubset[]
```

У публічний експорт цей модуль не входив і не входить, тож звичайного застосунку
це не чіпає. Але якщо ти діставав шрифт **імпортом із `vendor/`** (а так
доводилося робити, щоб порахувати ширину рядка), — він щойно зламався, рівно як
і мало бути. Заміна є з попереднього релізу: `@altera/server/print/metrics`.

---

## server 0.17.1 · tools 0.13.1 · skills 0.1.17 · create 0.7.10

### Змінилося саме

**Метрику тексту бланка можна отримати назовні — тепер верстку друку перевіряють
пробою, а не поглядом на папір.**

Бланк псується мовчки: число, ширше за комірку, ламається на два рядки, а
заголовок, чиє СЛОВО ширше за колонку, не переноситься взагалі й лізе на
сусідню. Ані `sql:publish`, ані валідність шаблону, ані правильні дані цього не
показують.

```ts
import {
  createPrintTextMeasurer,
  PRINT_CELL_PADDING,
  printContentWidth,
} from "@altera/server/print/metrics";

const measure = await createPrintTextMeasurer();          // ОДИН раз на прогін
const usable = printContentWidth() * 0.10 - PRINT_CELL_PADDING * 2;   // колонка 10 %

measure("1 234 567.89", 9) <= usable;
caption.split(/\s+/).every((word) => measure(word, 9, true) <= usable);
```

Заголовок міряй **по найдовшому слову**, а не по рядку: перенос іде по словах,
тож рядок із двох коротких слів розкладеться на два, а одне довге — ніяк.

Якщо ти вже писав свій вимірювач, його можна викидати — і головне, разом із ним
**імпорти з `vendor/`**. Дістатися до шрифту інакше було ніяк
(`@altera/server/print` його не віддавав), а такий імпорт ще й вимагає
`--no-config` при `"vendor": true` і мовчки ламається, щойно ядро перейменує
модуль. Тепер вхід публічний.

Вхід окремий від `@altera/server/print` навмисно: метрика тягне pdf-lib, а
`./print` імпортує екран редактора шаблонів — pdf-lib поїхав би в бандл
фронтенду.

---

## client 0.12.2 · skills 0.1.16 · create 0.7.9

### Потребує дії

**`<ui-select>` показував ПЕРШИЙ пункт замість збереженого. Перевір свої
`<select>` — найпевніше в тебе те саме.**

Виглядало це не як «показано не те», а як «**не збереглося**»: після вибору
контрол тримає вибране, і різниця вилазить аж після перезаходу. Шукати йдуть у
права доступу, payload і SQL — усе справне.

Причина в лита, і вона стосується будь-якого `<select>`, не лише компонента
ядра: прив'язки елемента комітяться раніше, ніж додаються його діти, тож
`.value=${…}` присвоюється, коли жодного `<option>` ще немає. Браузер таке
значення відкидає, а щойно пункти з'являються — стає на перший. Далі лит
`.value` не чіпає: воно дорівнює тому, що він сам поставив минулого разу.

```ts
// Неправильно — мовчки показує перший пункт
html`<select .value=${this.accountType} @change=${…}>
  <option value="active">Активний</option>
  <option value="passive">Пасивний</option>
</select>`

// Правильно — вибране на самому пункті, від порядку не залежить
html`<select @change=${…}>
  <option value="active"  ?selected=${this.accountType === "active"}>Активний</option>
  <option value="passive" ?selected=${this.accountType === "passive"}>Пасивний</option>
</select>`
```

Помилка видна ЛИШЕ коли збережене значення не збігається з першим пунктом, а в
половини списків перший пункт — найчастіший («20%», «покупка», «активний»). Тому
шукати варто не там, де щось не працює, а в **кожному** своєму `<select>`: у нас
із трьох знайдених місць запис знав про одне.

Порядок властивостей у ВИКЛИКУ (`.options` перед `.value`) не рятує — значення
виставляє сам компонент у своєму шаблоні.

У власному компоненті, де значення міняється ще й програмно (кнопка очищення,
перезавантаження запису), `?selected` мало: атрибут діє, лише доки в `<option>`
не зведено прапорець «брудності», а взаємодія його зводить. Там значення
доставляють властивістю після рендера — так тепер зроблено в `ui-select`:

```ts
protected override updated(changed: Map<string, unknown>) {
  super.updated?.(changed);
  const select = this.renderRoot.querySelector("select");
  if (select && select.value !== this.value) select.value = this.value;
}
```

Правило дописане в скіл `lit` — проби на нього немає й бути не може, це
поведінка DOM, тож єдине, що тримає його від повторення, це записаний текст.

---

## server 0.17.0 · client 0.12.1 · tools 0.13.0 · skills 0.1.15 · create 0.7.8

### Потребує дії

**Журнал документів дістав відбір за організацією — одним рядком.**

```ts
// у журналі документів
protected override organizationFilter = true;
```

Доти журнал фільтрувався за періодом і проведенням «задарма», а за організацією
— ніяк: колонка `organization_id` живе в `app.document`, тобто в ядрі, і
застосунку не було куди повісити `x-filter`. Поки організація в базі одна,
цього не видно; на другій журнал перетворюється на суміш двох обліків, і
відсіяти чужий нічим.

База дає три поведінки, і кожну варто знати: **умовчання — поточна
організація** (сіється до першого завантаження, зайвого запиту немає); це
**умовчання, а не заборона** — «усі організації» у списку й «Скинути» його
знімають; **при одній організації відбору немає взагалі**.

**Щоб це працювало, застосунок мусить зареєструвати контекст** — інакше
фреймворк поводиться так, наче організація одна, і відбір не з'явиться:

```ts
// app/main.ts, після відновлення сесії
import { setOrganizationContext } from "@client/shared/organization-context.ts";

setOrganizationContext({ current: () => currentOrg(), list: () => knownOrgs() });
```

Свого поняття організації фреймворк не заводить навмисно: «поточна організація»
це облікова політика застосунку — його шапка її показує, його сховище зберігає.
Тому вона приходить ЗЗОВНІ, тим самим прийомом, яким `bootstrap()` отримує все
на сервері. Обидва методи читаються на кожен рендер, тож перемикання
організації в шапці доходить саме собою — якщо за ними стоять сигнали.

`list()` у нас віддає перелік, завантажений один раз при вході. Якщо в тебе він
лежить у стані компонента шапки (а спершу так і було), його доведеться підняти
в спільний модуль — інакше фреймворк не має звідки дізнатися, скільки
організацій, і відбір не з'явиться, доки не відкрити меню вибору.

**Логічне поле без `default` у схемі більше не стає `true` у згенерованому
SQL.** Перегенеруй моделі (`deno task sql:gen`) і подивись у дифф.

Умовчання для `coalesce` генератор бере зі схеми, і все, крім явного `false`,
ставало `true` — `undefined` теж. Поле, якого немає в payload (а його не буде в
кожного клієнта, що шле лише заповнене), мовчки ставало «так»:
`amountIncludesVat` без ключа перетворював рахунок на «ціни з ПДВ», тобто з
іншою сумою до сплати. Нічого цього не ловило — SQL валідний, тип правильний,
проба на документі з явним ключем проходить.

Тепер `false`, і це не «краще значення», а єдине, яке не вигадує факт: «нема
ключа» означає «не сказано», а не «так». Хто хоче «так» — оголошує
`default: true` у схемі. У нашому дереві перегенерація дала **нульовий** дифф
(усі Item-схеми умовчання вже оголошують), тож найпевніше зміниться й у тебе
нічого — але подивитися варто саме там, де умовчання не оголошене.

**Текстовий блок бланка навчився прив'язки — і твої шаблони, найпевніше, уже її
пишуть.** Перевір бланки на прочерк там, де має бути значення.

Доти `text` умів лише статику, а `field-list` завжди друкував «підпис:
значення». Разом це означало, що ДИНАМІЧНИЙ рядок без підпису надрукувати не
було чим: заголовок бланка («Рахунок на оплату № 12 від 02.02.2026 р.»),
підсумок, «Надруковано …», реквізити сторони — усе це значення без підпису.

Гірше було те, що межа мовчала: шаблон писав `path` у текстовий блок, наче він
працює, нормалізація його викидала, і на бланку лишався **«-»**. Тобто
відсутність механізму виглядала як заповнений бланк, і побачити це можна було
лише поглядом на папір. У самому scaffold так друкувалася «Картка контрагента»
— рядок «Надруковано» стояв прочерком із першого дня.

Тепер працює і те, і те:

```json
{ "type": "text", "path": "document.title" }
{ "type": "text", "path": "document.total", "format": "amountInWords" }
{ "type": "field-list", "items": [{ "key": "org", "label": "", "path": "org.name" }] }
```

Порожній `label` друкує саме значення — без нього рядок починався з двокрапки.
Статичний `value` перекриває прив'язку: одне правило на весь формат, те саме, що
в комірці таблиці й у штрих-коді.

Міграції шаблонів **не треба** — навпаки, ті, що вже писали `path`, почнуть
друкувати значення замість прочерку. Що варто зробити руками: пройтися бланками
й зняти обхід, якщо в команді даних з'явилися шматки фраз (`titleTail`,
`itemsSummary` — підпис, дописаний до значення заради `field-list`). Такі поля
тепер зайві, а команда даних перестане бути описом розкладки.

**Умовна видимість у шаблоні: `visibleWhen`.** Бланк більше не треба звужувати
до спільного знаменника й не треба множити на комбінації.

```json
{ "type": "field-list", "visibleWhen": "invoice.hasVat", "items": [ … ] }
{ "key": "c_vat",  "widthPercent": "10", "visibleWhen": "invoice.hasVat" }
{ "key": "r_note", "cells": [ … ],       "visibleWhen": "note" }
{ "key": "vat", "label": "У т.ч. ПДВ", "path": "vat", "visibleWhen": "hasVat" }
```

Поле лежить на **всіх** рівнях: блок будь-якого типу (лінії, картинка й
штрих-код включно), колонка таблиці, рядок будь-якої секції, рядок списку
полів. «Включно з лініями» — не для повноти: підвал із факсиміле це картинка
ПЛЮС риска, і сховати з них частину означало б лишити висячу лінію.

Корінь шляху — той самий, що в `path` поруч: дані друку для блоків, колонок,
рядків шапки й підвалу — і **запис** для рядка секції `row`.

Прапорець рахує КОМАНДА ДАНИХ (`total_vat > 0 as "hasVat"`), бо умова це шлях,
а не вираз: шаблон редагує бухгалтер, і мова всередині форми там зайва.
Рішення лишається в SQL, бланк лише показує, чим воно керує.

Хиба — `false`, `0`, `""`, `[]`, `null`, відсутнє поле, **і рядки `"false"` /
`"0"`**: команда даних, що віддає все текстом, інакше показувала б блок завжди,
а побачити це можна було б лише на папері.

Порожня умова = «видно завжди», тож наявні шаблони чинні **без міграції**.

Дві речі, які не треба компенсувати руками: схована колонка віддає ширину
сусідам сама, а комірка, об'єднана через неї, звужується (`colSpan: 3` → `2`), а
не зникає. Рядок, від якого не лишилося комірок, не друкується; таблиця без
жодної видимої колонки не друкується зовсім.

**Порожнє значення тепер друкується порожнім, а не прочерком.** Це помітно на
кожному бланку, тож варто подивитися на папір.

Раніше `stringifyPrintTemplateValue` віддавав «-» на `null`/`''`, і це було
втручанням ядра в бланк: «нічого» на регламентованій формі виглядає по-різному —
десь порожньо, десь прочерк, десь «б/н», — і вирішує це той, хто форму робить.
Найвидніше було в таблиці: у рядка-послуги немає одиниці виміру, і колонка «Од.»
друкувалася прочерком на кожному такому рядку.

Кому прочерк потрібен — ставить його **в команді даних**, де відомо, чи поле «не
заповнене», чи «не застосовне»:

```sql
coalesce(nullif(u.short_name, ''), '-') as "unit"
```

Непридатне значення (об'єкт, масив у скалярній прив'язці) теж дає порожнє:
позначкою прочерк однаково не був — помилку прив'язки видно з того, що на папері
немає значення.

**Картинка теж дістала прив'язку — логотип, печатка й факсимільний підпис
нарешті можливі.**

```json
{ "key": "stamp", "type": "image", "path": "invoice.sellerStamp", "alt": "Печатка" }
```

Робиться це разом із текстом навмисно: після цих двох у форматі не лишилося
блоків, які «майже вміють» брати значення з даних. Причина не в симетрії —
логотип і печатка належать ОРГАНІЗАЦІЇ, від імені якої друкують, а організацій
у базі кілька; статичний `src` означав би одну печатку на всі, тобто не
спрощений документ, а неправильний. Команда даних віддає `data:`-URI рядком.
Порожня прив'язка дає порожній `src`, а не «-»: прочерк тут був би зламаною
картинкою замість відсутньої.

**У типах формату з'явилися нові ОБОВ'ЯЗКОВІ поля.** Якщо в тебе є свій редактор
шаблонів (екран `admin/print_template` scaffold не везе, тож це копія), фабрики
блоків перестануть компілюватися:

| тип | нові поля |
|---|---|
| `PrintTemplateBlockBase` (усі блоки) | `visibleWhen` |
| `PrintTemplateTextBlock` | `path`, `format` |
| `PrintTemplateImageBlock` | `path` |
| `PrintTemplateTableColumn` | `visibleWhen` |
| `PrintTemplateTableRow` | `visibleWhen` |
| `PrintTemplateFieldListItem` | `visibleWhen` |

```ts
// app/admin/print_template/printTemplate.blocks.ts
{ key: newKey(), type: "text", style: "body", value: "Текст",
  path: "", format: "", visibleWhen: "",     // ← додати
  placement: …, text: … }

{ key: newKey(), type: "image", src: "", path: "", visibleWhen: "", alt: "", … }
{ key: newKey(), widthPercent: "20", visibleWhen: "" }              // колонка
{ key: newKey(), label: …, path: "", format: "", visibleWhen: "" }  // поле списку
{ key: newKey(), cells: [ … ], visibleWhen: "" }                    // рядок секції
```

Дві пастки в перебудові сітки, які легко пропустити: `gridToRows()` збирає
рядки заново — умову треба **перенести з вихідного рядка**
(`visibleWhen: sourceRows[rowIndex]?.visibleWhen ?? ""`), інакше об'єднання
комірок її мовчки стирає; те саме в `cloneBlock()` (`{ ...row, key: newKey() }`
замість перелічення полів).

У панелі властивостей додай «Показувати, коли» на чотирьох рівнях (блок, поле
списку, колонка, рядок секції) і прив'язку для тексту й картинки — усе це вже є
поруч у блоці штрих-коду й копіюється рядок у рядок. Для картинки додай ще й
кнопку «прибрати файл»: доки `src` непорожній, він перекриває прив'язку, а зняти
його не було чим.

`isPrintTemplateElementVisible` експортується з `@altera/server/print` — клич
його з полотна замість власної копії правила істинності. Полотно, яке рахує
«видно» інакше, ніж рендерер, обіцяє те, чого на папері не буде.

Далі — дві дрібниці, що вилізли на вкладках із попереднього релізу. Обидві —
вигляд, не дані; обидві прибирають обхід, який довелося написати.

**Панель дій табличної частини перемальовується сама, коли міняється `readonly`.**
Прибери обхід після `post`/`unpost`, якщо писав:

```ts
// більше не потрібно
for (const section of this.sections()) section.refresh();
```

Було так: `section.readonly` — це функція форми (`() => this.readonlyMode`), а та
читає сигнали (право на запис і `$root.item.isPosted`). Форма перемальовувалася
одразу, а `<ui-tabular-toolbar>` — ні: його власна властивість `section`
лишалася тим самим об'єктом, і lit не мав про що дізнатися. Тому після
«Розпровести» кнопки лишалися вимкненими, доки не перемкнеш вкладку — стан у
формі вже правильний, неправильне лише те, що видно. Тепер тулбар слухає сигнали
так само, як `<ui-tabular-table>` із самого початку.

`section.refresh()` нікуди не подівся — він і далі потрібен для стану ПОЗА
`$root` (кеш конфігурації, який сигналами не трекається).

**Зазор між панеллю дій і таблицею у вкладці ставить тема.** Прибери своє
правило, якщо додавав:

```css
/* більше не потрібно */
.form-tab-panel ui-tabular-toolbar { margin-bottom: 6px; }
```

Доти цей зазор давала розмітка форми — обгортка `<div class="… mb-1">` навколо
тулбара. Коли панель поїхала у `<ui-form-tabs>`, обгортка пішла разом із нею, а
відступ за собою не забрала: над кнопками 6px, під ними 0, і рамка таблиці
сходилася зі смугою дій в одну лінію. Тепер це `.form-tab-panel >
ui-tabular-toolbar { margin-bottom: 6px }` у темі. Величина та сама, що
`padding` панелі, і саме `margin`, а не `gap`: маргін із твоїм правилом не
складеться, `gap` склався б і дав подвійний зазор.

Форми БЕЗ вкладок це не зачіпає — там тулбар і таблицю ставить розмітка
застосунку, і зазор лишається її справою.

### Змінилося саме

**Зірочка обов'язковості стоїть удвічі ближче до підпису — 4px замість 8.**
Робити нічого не треба: і `renderField()`, і підписи компонентів (`ui-picker`,
`ui-date`, `ui-decimal`, `ui-select`, `ui-image`) малюють її самі.

Вісім пікселів набігали з двох джерел і жодне з них не було наміром: `.label` у
daisyUI 5 — це `inline-flex` із `gap: .375rem`, а розмітка додавала зверху ще
`ml-0.5`. На вузькому контролі підпис із позначкою заходив на сусідній —
`.label` несе `white-space: nowrap`, тож зайве не переноситься, а вилазить.

Заразом позначка дістала СВІЙ клас — `<span class="field-required">*</span>`
замість `text-error ml-0.5`, розсипаного по десятьох місцях; вигляд тепер у
темі (`.field-required` і `gap` підпису). Якщо десь малюєш зірочку своїми
руками, став цей клас — інакше поле лишиться зі старим зазором і поїде повз
решту форми:

```ts
${required ? html`<span class="field-required">*</span>` : ""}
```

---

## client 0.12.0 · tools 0.12.1 · skills 0.1.14 · create 0.7.7

### Потребує дії

**Смуга вкладок для кількох табличних частин — у ядрі: `<ui-form-tabs>`.**
Прибери свою, якщо писав.

```ts
import "@client/ui-kit/tabular/ui-form-tabs.ts";

html`<ui-form-tabs .tabs=${[
  { key: "goods",    titleKey: "supplierInvoice.goods",    section: this.goods },
  { key: "services", titleKey: "supplierInvoice.services", section: this.services },
  { key: "extra",    titleKey: "common.extra",             content: () => this.renderExtra() },
]}></ui-form-tabs>`
```

Вкладка з `section` малюється цілком — панель дій, таблиця, кількість рядків,
позначка помилки; `content` лишається для вкладки, у якій не таблиця, а звичайні
поля. Малюється тільки активна панель (рядки живуть у `$root`, тож сховане нічого
не губить).

Дві речі компонент бере на себе, і саме через них це місце ядра:

- **вкладка з помилкою вмикається сама.** `BaseUI` перевіряє ВСІ секції з
  `sections()`, включно зі схованими, тож банер казав «Рядок 2, «Сума»…», коли
  рядка на екрані немає. Ловиться це наявним сигналом (`pendingFocus`, який база
  ставить першій секції з помилкою), а не звіркою `errorCount`: інакше
  користувача тягло б назад щоразу, коли він пішов подивитися сусідню частину;
- **вкладка знову може бути `<button>`.** Своя смуга в розмітці форми лежить
  усередині `fieldset[disabled]` з `renderFields()`, і каскад гасить усі нативні
  контроли — проведений документ переставав гортатися. Каскад не перетинає межу
  тіньового кореня, тож усередині компонента кнопка працює, а з нею клавіатура й
  доступність.

Забирай і свої класи `.form-tab*` зі збірки Tailwind — вони тепер у темі
фреймворку (`.form-tabs`, `.form-tab`, `.form-tab-count`, `.form-tab-error`,
`.form-tab-panel`).

**Панель дій табличної частини в режимі перегляду більше не зникає — вона
вимкнена.** Те саме з колонкою кошика в рядку. Зникнення читалося як поламка:
вимкнена кнопка каже «дія тут є, зараз не можна», відсутня — «дії тут немає
ніколи», а для щойно проведеного документа правильне саме перше. Плюс проведення
не перезавантажує форму, тож смуга з'являлася й зникала під курсором, зсуваючи
таблицю. Кому потрібне саме зникнення — той не ставить `<ui-tabular-toolbar>`
(компонент незалежний) або віддає секції `rowDelete: false`.

### Змінилося саме

**Об'єкт-ссылка в схемі більше не вимагає `x-transient`.** Поле
`nomenclature: { id, name }` поруч із `nomenclatureId`, що несе `x-ref`,
генератор тепер упізнає сам і колонки під нього не шукає. Доти воно розбиралося
як звичайне поле, і публікація падала аж на `create function`:

```
column l.nomenclature does not exist
```

— причому `sql:gen` і `deno check` перед цим були зелені. Анотацію лишай, де
вона вже стоїть: вона й далі каже читачеві «це не колонка».

**Скіли: домовленості про документи**, кожна з яких коштувала переробки на
першому прикладному рішенні —

- `post` — це СТАН, а не тільки рухи: документ без жодної проводки (рахунок на
  оплату, платіжне доручення) усе одно проводиться, а `post_entries` у нього
  не відмовляє, а перевіряє;
- табличних частин стільки, скільки їх у предметній області: вони окремі не
  тому, що їх не можна звести в одну, а тому, що так зручніше заповнювати людині;
- зміна складу табличних частин — це міграція (публікація SQL нічого не зносить);
- колонку `not null default` треба передавати явно: у `MERGE` відсутній ключ
  приходить явним `null`, і умовчання колонки не спрацьовує.

## server 0.16.0 · tools 0.12.0 · skills 0.1.13 · create 0.7.6

### Потребує дії

**Сума прописом — у ядрі, форматом поля шаблону друку.**

```json
{ "schemaVersion": 2, "locale": "uk", "currency": "UAH", "blocks": [
  { "type": "field-list", "items": [
    { "key": "sum", "label": "Сума словами", "path": "document.total", "format": "amountInWords" }
  ]}
]}
```

`format` працює на елементі `field-list` і на комірці таблиці; мова й валюта —
у корені шаблону (умовчання `uk`/`UAH`, тож наявні шаблони чинні без міграції).
Мови: `uk`, `ru`, `en`.

**Прибери свою функцію**, якщо писав: правило належить мові, а не застосунку, і
помиляються в ньому однаково — рід належить розряду («дві тисячі», але «два
мільйони»), форму вибирають дві останні цифри (111 — «гривень»), порожній
розряд усе одно називається. На 1234,56 не видно жодної з цих помилок.

Рахувати суму словами в команді даних більше не треба: прив'яжи число, а як його
надрукувати, каже шаблон — те саме поле можна вивести і цифрами, і прописом.
Валюта з неоголошеними формами **відхиляється** й друкується числом (як помилкове
значення штрих-кода), тож перевіряй надрукований результат, а не факт виклику.

**Тим, хто конструює блоки шаблону в TypeScript**, доведеться поправити типи:
`PrintTemplateFieldListItem` і `PrintTemplateTableCell` дістали обов'язкове поле
`format` (порожній рядок — без формату). Саме через це сервер іде мінором.

**`x-ref` бачить моделі, чий SQL не генерується.** Карта посилань будувалася з
`app/sql.json`, тобто з переліку тих, хто везе СВІЙ SQL, — і посилання на
`user` чи `user_group` падало з «модель не знайдена». «Мій SQL пишуть інші» та
«на мене не можна посилатися» — різні речі.

Тепер карта будується з манифестів усього дерева (той самий обхід, що й у
реєстру рантайму). Плюс манифест приймає ім'я таблиці, коли воно не збігається з
ключем моделі:

```json
{ "model": "user", "table": "users" }
```

**Перевір імена файлів схем:** генератор шукає `<model>.schema.ts` і мовчки
пропускав модель, якщо файл названо інакше (`userGroup.schema.ts`). Тепер пропуск
друкується рядком у `--verbose`, але сама вимога не змінилася — модель зі схемою
під іншим іменем у карту посилань не потрапляє.

### Тільки для нових застосунків

**Піни `@altera/tools` у `Dockerfile` і `docker-compose.yml` шаблону підняті до
поточних.** Вони застрягли на `^0.5.x` при `0.11.x`: каретка на `0.x` не пускає
далі наступного мінора, тож контейнерний `solution:update` ішов інструментом
піврічної давнини проти свіжого сервера — та сама розбіжність версій, через яку
вже був фікс-реліз, лише в місці, куди ніхто не дивився.

Наявних застосунків це не стосується — шаблон їде тільки в нові. **Якщо ти
розгортаєш у контейнері**, поправ у себе ці два рядки руками; шукати їх за
`jsr:@altera/tools@`.

Проба пінів тепер читає й ці два файли — вона сканує їх текстом, тож формат
значення не має. Доти перевірялися лише `deno.json`, і саме тому дрейф не було
видно.

---

## client 0.11.2 · tools 0.11.1 · skills 0.1.12 · create 0.7.5

### Потребує дії

**Оновіться до цих версій, якщо встигли поставити попередні** — `client 0.11.1`,
`tools 0.11.0`, `skills 0.1.11`, `create 0.7.4` пішли в реєстр із коміту, ЯКИЙ
ЩЕ НЕ МІСТИВ вмісту релізу нижче. У них немає ані експорту `popover.ts`, ані
правильного порядку сідів, а шаблон scaffold і сам `tools` пінять `@altera/server@^0.14.0`
— тобто у встановленому застосунку опинялися ДВІ версії сервера: схема з однієї,
рантайм з іншої.

`@altera/server@0.15.0` виправляти не треба — він опублікований із правильного
коміту й містить усе, що описано нижче.

Пастка тут не в коді, а в порядку: **публікація йде з того коміту, на якому
запущений workflow**, а версії, вже наявні в реєстрі, вона мовчки пропускає. Тож
запуск зі старого коміту забирає номери, а наступний, правильний, їх уже не
чіпає — і побачити це можна лише кроком `scaffold:verify` після публікації
(він і побачив: «у графі застосунку 2 версії @altera/server»).

---

## server 0.15.0 · client 0.11.1 · tools 0.11.0 · skills 0.1.11 · create 0.7.4

### Потребує дії

**З'явилося спільне місце для налаштувань — пакет ядра `@core/setting`.** Додайте
його в `app/sql.json` (одразу за `@core/audit` — залежить лише від `app.users`),
інакше публікація впаде на відсутній `app.setting_value`.

Одна таблиця на обидві області: `user_id is null` — значення установки, інакше —
значення людини. Порядок накладання названий один раз (`app.setting_read`):
значення користувача сильніше за значення установки. Користувацька область поки
не ввімкнена, але колонка є від першого дня — додавати її потім означало б міняти
ключ уже наповненої таблиці.

**Що можна налаштувати, каже застосунок:** рядки сіє його `db/data.sql` через
`on conflict do nothing`, а значення після першого сіву належить адміністратору й
перевикочування його не затирає — той самий поділ, що в нумераторах, шаблонах
друку й рівнях журналу. Зворотний бік той самий: змінене в сіді умовчання на вже
налаштованій базі **не застосується**.

Читати з SQL — `app.setting_read(user_id, key)`; усе одразу мапою —
`app.setting_effective`. Екран у застосунку робиться звичайною admin-моделлю; у
цьому репозиторії еталон — `app/admin/setting/`.

**Розріз за субконто більше не губить рухів без цього субконто — і числа у
звітах зміняться.** `app.acc_balance_turnover_by_dim` відбирав значення виміру
через `cross join lateral`, тобто рух, у якого цього субконто немає, зникав із
результату **цілком** — не ставав рядком із порожнім значенням, а зникав.

Помилка мовчить уся: функція не падає, кожен рядок окремо правильний,
неправильна лише їхня СУМА — величина, якої ніхто не звіряє з рахунком. На
даних altera-buh рахунок 311 давав 117 500 через `acc_balance_turnover` і 5 500
у розрізі статті руху коштів; різниця — початкові залишки, де статті руху немає
й бути не може.

Суть у тому, що субконто веде не рахунок, а ПРОВОДКА, і ядро саме це допускає:
`chart_of_account_analytic.is_required` дозволяє необов'язковий слот. Тобто шар
читання суперечив власному правилу схеми.

Тепер рух без виміру приходить окремим рядком із `value_id is null` — останнім
(`null` в ASC сортується в кінець). **Перевірте свої звіти, що беруть розріз:**
сума рядків тепер дорівнює сальдо рахунку, тож підсумки зростуть, а в аркуші
з'явиться рядок без значення виміру. Назвати його «&lt;не вказано&gt;», згорнути чи
відсіяти — вирішує звіт: шар віддає рух, а не подання.

Повернутися це не може — інваріант «сума розрізу = підсумок рахунку» тепер під
пробою (`deno task smoke`, крок «регістр: розріз за субконто не губить рухів без
нього»).

**Підбір можна звузити відбором — і невідомий ключ він тепер ВІДХИЛЯЄ.**
Половина механізму була давно: `<ui-picker>` слав параметри в payload. Другої не
було — генератор складав фільтри лише для `_list`, тож у `_lookup` вони мовчки
ігнорувалися, і на екрані це виглядало як «відбір не працює», а не «відбору
немає».

Тепер `_lookup` бере ті самі оголошення `x-filter`, що й `_list`, а пікер веде
відбір однією властивістю в обидва шляхи вибору — випадний список і діалог:

```html
<ui-picker url="catalog/bank_account"
  .filters=${{ organizationId: item.organizationId }}
  .value=${item.bankAccount ?? null}
  @value-changed=${(e) => this.setRef("bankAccount", e.detail.value)}
></ui-picker>
```

**Перегенеруйте моделі, чий підбір має звужуватися** (`deno task sql:gen
<family>/<model>`), і оголосіть потрібні поля `x-filter` у схемі, якщо їх там ще
немає.

**Що може зламатися на місці:** якщо ваша форма вже слала в підбір параметри —
через `fetch-params` або `picker-params`, — і серед них є ключ, якого модель не
оголошує, підбір тепер **відмовляє**. Доти він мовчки віддавав повний перелік.
Це навмисно: форма, яка звузила підбір, вважає, що звузила його, а друкарська
помилка в імені фільтра не лишала жодного сліду. Модель, яка фільтрів не
оголошує, відмовляє на будь-якому наборі — з тієї самої причини.

Відбір підбору **не є** фільтром списку, хоч і користується тим самим
оголошенням: у списку його задає користувач панеллю й може зняти, тут — форма, і
зняти його не можна. Рахунок своєї організації в полі «рахунок платника» — не
звужений вибір, а помилка вводу.

### Змінилося саме

**Розміщення випадного вікна доступне застосунку — `@altera/client/ui-kit/popover.ts`.**
Модуль був у пакеті й робив рівно потрібне (`computePlacement` + `placePopover`:
відкриття вгору, коли внизу не влазить, і притискання до краю вікна), ним
користуються `ui-picker`, `ui-date` і `ui-period` — але в карті експортів його не
було, і своє меню застосунок розміщував власною математикою. Тобто виправлення,
зроблене саме тому, що троє контролів рахували позицію кожен по-своєму й однаково
неповно, було сховане від четвертого охочого.

Разом із розрахунком їде `POPOVER_ANCHORED_STYLE` — скид, без якого браузер
центрує `[popover]` у в'юпорті власними `inset: 0` + `margin: auto`, і задані
`top`/`left` не діють узагалі:

```ts
import { placePopover, POPOVER_ANCHORED_STYLE } from "@altera/client/ui-kit/popover.ts";

html`<div popover="manual" style=${`${POPOVER_ANCHORED_STYLE} width:20rem;`}>…</div>`
// показати, і ТІЛЬКИ ПОТІМ розміщувати: у схованого вікна немає розмірів
popover.showPopover();
placePopover(popover, anchor, { desiredHeight: 320 });
```

Доти цей рядок стилю був переписаний слово в слово в трьох контролах фреймворку;
тепер він один, і застосунок бере той самий.

**Типи документів і правила нумерації тепер сіються ПЕРЕД сідами застосунку.**
Доти згенеровані секції йшли в самий кінець секції даних, тож сід, який заводить
документ викликом `<model>_save` — саме так, як радить документація, — падав на
порожній базі:

```
Тип документа «expense_report» не зареєстровано в app.document_type
```

Гірше за саму помилку було те, що вона **самозакривалася**: публікація
зупинялася до реєстрації типів, тож наступна впиралася в те саме, і вийти з кола
можна було лише правкою сіду або ручним `insert`. На чистій установці (перший
`sql:publish` у замовника) у це впирався б кожен, чий сід заводить документ.

Дорогою з'ясувалося, що сід нумератора робив **дві різні речі одним блоком**, і
місця в пакеті в них протилежні: оголошення правила потрібне ДО сідів (сід бере
з нього номер), а `numerator_reseed` — ПІСЛЯ них (він підтягує лічильник під
рядки, які сід вставив прямим `insert` із власним кодом). Тому в зібраному
пакеті тепер дві секції замість однієї — `_generated/numerators.data.sql` на
початку й `_generated/numerators-reseed.data.sql` наприкінці.

Робити нічого не треба — досить перезібрати пакет (`deno task sql:assemble`).
Порядок під пробою: `tools/assemble-sql-package_test.ts`.

**Закрита вкладка повертає в ту, з якої її відкрили.** Нова вкладка стає
останньою в смузі, тож сусід ліворуч від неї — це те, що відкривали перед нею, а
не те, куди користувач збирається повернутися: відкривши об'єкт зі списку
посеред смуги, після закриття він опинявся у випадковій чужій вкладці, а не в
своєму списку з відбором і позицією курсора. Тепер вкладка пам'ятає, з якої
народилася, і закриття веде туди; позиційний вибір лишається запасним — для
вкладки, чий «батько» уже закритий, і для відновлених після перезавантаження
(id там генеруються заново, тож зв'язок було б вигадано). Робити нічого не
треба.

**Смуга вкладок більше не прокручується — те, що не вміщається, іде під кнопку
історії** в правому кінці смуги. Доти зайві ярлики виїжджали за екран і
з'являлася горизонтальна прокрутка; разом зі зникненням кнопки «Закрити» у формі
це означало, що таку вкладку не закрити взагалі, доки не докрутиш до неї.

Ховається найдавніше вживана — той самий порядок, що й у витісненні, тож вкладка
спершу зникає зі смуги й лише потім, дійшовши до ліміту в 30 відкритих,
закривається насправді. Активна не ховається ніколи. Приховані лишаються ЖИВИМИ:
дані завантажені, стан форми на місці — по кліку в переліку вкладка просто
повертається у смугу. Там же її можна й закрити, не відкриваючи; якщо серед
прихованих є незбережена, на кнопці стоїть «*». Робити нічого не треба.

---

## server 0.14.0 · tools 0.10.1 · skills 0.1.10 · create 0.7.3

### Потребує дії

**Шар обчислень над регістром переїхав у ядро — `@core/ledger`.** Підсумків
система не зберігає, тож сальдо й обороти рахуються скануванням
`app.journal_entry`; доти це робив кожен звіт власним запитом, і методологія вже
двічі розходилася — вхідне сальдо при відкритій даті початку ставало ще й
оборотом, а кінцеве виходило вдвічі більшим (на екрані не видно: `<ui-period>`
завжди підставляє період).

Додайте пакет у `app/sql.json` — **після** `@core/document_core` і після свого
плану рахунків:

```jsonc
"@core/ledger",
```

Що він дає:

| Функція | Рядок — це | Кому |
|---|---|---|
| `app.acc_entries(org, from, to, accounts, dims)` | **бік** проводки | картка рахунку, будь-який погляд ВІД РАХУНКУ |
| `app.acc_journal(org, from, to, accounts, dims, document_id)` | **проводка**, обидва боки | журнал проводок, рух документа |
| `app.acc_balance_turnover(org, from, to, accounts, dims)` | рахунок | оборотка: вхідне, обороти й кінцеве за один прохід |
| `app.acc_balance` / `app.acc_turnover` | рахунок | одна величина |
| `app.acc_balance_turnover_by_dim(…, dimension)` | значення субконто | аналіз субконто |
| `app.acc_account_tree(code)` | — | рахунок разом із субрахунками |

Два правила, заради яких шар і існує: **що вважається рухом** (проведений,
непозначений, тієї організації) сказано один раз — в `acc_entries`; **вхідне
сальдо з відкритою датою початку дорівнює нулю**, тому беруть його з
`acc_balance_turnover`, а не з `acc_balance(org, date_from)`.

Подання шар не робить навмисно: віддає чисте `net` (дебет − кредит), а
розкладати його на дебетову й кредитову колонки — справа звіту.

**Свої запити по регістру можна викидати.** У трьох звітах цього репозиторію
після переходу не лишилося власної арифметики; в `app/report/turnover_balance` і
`app/report/account_card` це прибрало дубль методології, в
`app/report/document_movements` — третій дубль складання субконто в масив.
Числа звірені до й після: збігаються один в один.

---

## server 0.13.0 · client 0.11.0 · tools 0.10.0 · skills 0.1.9 · create 0.7.2

### Потребує дії

**`<ui-picker>` приймає ссылку ОБ'ЄКТОМ — контракт замінено, старий прибрано.**
Значення тепер одне (`value`), а не пара «id окремо, підпис окремо», і подія
одна (`value-changed`) — та сама, що в `ui-date` і `ui-decimal`:

```html
<!-- було -->
<ui-picker url="catalog/counterparty" fetch="lookup"
  .selectedId=${item.counterpartyId ?? ""}
  .displayValue=${item.counterparty?.name ?? ""}
  @item-selected=${(e) => { … два записи … }}
  @item-cleared=${() => { … ще два … }}
></ui-picker>

<!-- стало -->
<ui-picker url="catalog/counterparty"
  .value=${item.counterparty ?? null}
  @value-changed=${(e) => this.setRef("counterparty", e.detail.value)}
></ui-picker>
```

Прибрано: `selectedId`, `displayValue`, `item-selected`, `item-cleared`,
`fetch`. Забутий пікер тепер **ламається голосно** (властивості немає, подія не
слухається), а не показує порожнє поле — саме заради цього старе прибрано, а не
лишено «поки що працюючим».

Чому об'єкт: підпис і так завжди приїжджав у відповіді — `x-ref` кладе вкладений
`{ id, name }` у `get`, у рядок списку, в эхо фільтра й у `lookup`. Пікер тепер
бере його як є, замість того щоб форма розбирала його на дві прив'язки й
збирала назад.

Що допомагає при переході:

- **`BaseUI.setRef(key, value)`** пише і сам об'єкт, і `<name>Id` — розсинхронити
  їх нема як. Ім'я id-поля виводиться як `<key>Id`, інакше третім аргументом;
- **`save` приймає обидва вигляди** — `counterpartyId` як раніше або
  `counterparty: {id}`. Тобто форма може тримати лише об'єкт;
- **у фільтрах змін майже немає**: значення ссылочного фільтра вже об'єкт, тож
  `.value=${this.filterValue("counterparty") ?? null}` і
  `@value-changed=${(e) => this.setFilter("counterparty", e.detail.value)}`;
- **у табличній частині — нічого**: колонку `picker` перевела сама база
  (`fetchCommand` з опцій колонки прибрано за непотрібністю).

Тип події експортується: `import type { PickerChangeEvent } from
"@client/ui-kit/components/ui-picker.ts"` — локальні `type PickEvent = …` у
формах більше не потрібні.

**Періодичні дані генеруються з оголошення.** Ключ, дата, значення — четвертий
типовий вид моделі після довідника, документа й регістру, і однаковий скрізь.
Тепер він оголошується в манифесті регістру:

```jsonc
"periodic": { "key": ["currencyId"], "period": "period" }
```

і `sql:gen` дає до звичайного CRUD ще три команди — `at` (зріз останнього на
дату), `history`, `set` (перезапис значення на дату, `merge` за природним
ключем) — плюс **унікальний індекс `(ключ…, період desc)`**. Індекс кладе сам
генератор: унікальність тримає `set`, а напрямок — те, що зріз стає пошуком, а
не скануванням. У `struc.sql` його писати не треба.

Права трьох команд виводяться звідти ж (`at`/`history` — `view`, `set` —
`edit`), і рантайм маршрутизує їх без запису в `commands.sql`.

Що це прибирає: перший такий регістр коштував ≈130 рядків рукописного SQL, і
кожен наступний коштував би стільки ж — зі своїм шансом розійтися з контрактом.
Еталон — `app/data/currency_rate/` у монорепозиторії.

**Проводка може мати порожній бік — але тільки на забалансовому рахунку.**
Забалансовий облік у плані рахунків України однобічний за визначенням: «Дт 021
Устаткування, прийняте для монтажу» не кореспондує ні з чим. Виразити це було
нічим — обидві колонки `app.journal_entry` були `not null`, а обхід (парна
проводка через допоміжний рахунок) псує самі дані: у регістрі з'являється
кореспонденція, якої в обліку немає, і звіт по кореспонденціях починає показувати
неіснуючі зв'язки.

Тепер `debit_account` і `credit_account` нульові, а зводить їх обмеження
`ck_journal_entry_sides` (хоча б один бік). `doc_entry_add` приймає порожній бік
і вимагає, щоб рахунок був `is_off_balance`: на балансовому рахунку однобічна
проводка — не задум, а недописаний рядок, і мовчки прийняти його не можна.

**Перевірте власні звіти по регістру.** Запит виду

```sql
select debit_account as account, amount from app.journal_entry
union all
select credit_account, amount from app.journal_entry
```

тепер збирає порожній бік в окремий рахунок «null» — з сумою, яка робить
підсумки «зведеними». Додайте `where … is not null` у кожен бік (в оборотці
цього репозиторію це зроблено). Заразом варто вирішити, що робити із
забалансовими рахунками в підсумку: тут вони лишилися рядком, але з суми
вилучені — інакше «баланс зійшовся» перестає щось означати.

Своєму документу, який веде забалансовий облік, лишається передати `null` одним
боком у `doc_entry_add`. У ручній операції це вже так: порожній один бік
проходить, порожні обидва — ні.

---

## server 0.12.0 · client 0.10.0 · tools 0.9.1 · skills 0.1.8 · create 0.7.1

### Потребує дії

**Документні моделі нарешті генеруються поза монорепозиторієм.** `sql:gen`
шукав схему спільної шапки за шляхом `app/../client/shared/schema.ts` — тобто за
розкладкою МОНОРЕПОЗИТОРІЮ, зашитою в інструмент, який роздається з JSR. У
встановленому застосунку каталогу `client/` немає й бути не може (фреймворк
лежить у `vendor/`, а `@client/` — аліас карти імпортів), тож `type: "document"`
не генерувався взагалі:

```
✗ operation/manual_entry: Module not found "file:///…/client/shared/schema.ts"
```

Тепер шапка береться звідти ж, звідки її бере сам застосунок — з його карти
імпортів (`@client/`, а без аліаса — `@altera/client`), тобто **версію називає
застосунок**, а не інструмент. Це навмисно не залежність `tools` від
`@altera/client`: інакше в застосунку опинилися б дві версії клієнта — та сама
пастка, що вже стріляла з `@altera/server`.

Що це означає на місці: документи, які доводилося заводити в монорепозиторії
фреймворку й копіювати `.gen.sql` руками, тепер генеруються у себе. Прогоніть
`deno task sql:gen <family>/<model>` по своїх документах — і звіряйте вихід із
тим, що лежить: копія могла відстати від схеми.

Якщо карти імпортів немає взагалі, відмова тепер називає причину замість
`Module not found`.

**Ядро більше не сіє виміри субконто й прив'язки до рахунків.** У
`@core/document_core` лежали ПРИКЛАДНІ дані: описи довідників застосунку
(`counterparty`, `bank` — з іменами його таблиць) і прив'язки до рахунків
українського плану (311–314, 361, 631…), причому з `on conflict … do update`.
Тобто ядро лишалося власником слота назавжди: застосунок, що ставив на 311 свій
вимір, після кожного `sql:publish` мовчки отримував назад ядровий — без помилки,
без сліду, помітно аж у проводці через місяць.

Тепер це оголошує застосунок, і в двох місцях за змістом:

```sql
-- app/catalog/bank/db/data.sql — вимір оголошує модель, чий довідник ним працює
insert into app.analytic_dimension (code, name, entity_kind, model_key,
  target_table, id_column, code_column, name_column, is_active) values
  ('bank', 'Банк', 'catalog', 'bank', 'app.bank', 'id', 'mfo', 'name', true)
on conflict (code) do nothing;

-- app/catalog/chart_of_account/db/data.sql — прив'язку до рахунків веде план
insert into app.chart_of_account_analytic (account_code, slot_no, dimension_code, is_required)
values ('311', 1, 'bank', true), …
on conflict (account_code, slot_no) do nothing;
```

**Наявні бази від оновлення нічого не втрачають** — рядки вже стоять, а `do
nothing` їх не чіпає. Але після оновлення **чиста** база отримає лише те, що
оголосив застосунок: якщо ви покладалися на сід ядра, перенесіть ці рядки до
себе, інакше проведення впаде на «рахунок веде субконто, якого немає».

**Порядок у `sql.json` став значущим:** модель, що оголошує вимір
(`catalog/bank`, `catalog/counterparty`), стоїть **вище** за план рахунків.
Нижче — і прив'язка не знайде виміру; зовнішній ключ зробить це помилкою
публікації, а не тихим пропуском.

Туди ж переїхало виправлення `code_column` виміру «Банк» (`bank` → `mfo`) — у
`app/catalog/bank/db/migration.sql`. У пакеті ядра імені прикладної таблиці
більше немає ніде.

**Гак «перед записом документа».** Заборона закритого періоду й будь-яка
перевірка «чи можна чіпати цей документ» тепер має одну точку: ядро кличе
`app.doc_before_write(p_user_id, p_op, p_doc, p_prev)` зі свого тригера на
`app.document`. Вмикається створенням функції — реєструвати нічого не треба:

```sql
create function app.doc_before_write(
  p_user_id bigint, p_op text, p_doc jsonb, p_prev jsonb
) returns void language plpgsql as $$
begin
  if p_op in ('insert', 'update', 'post', 'unpost')
     and (p_doc->>'doc_date')::date <= app.period_lock_date(p_user_id) then
    raise exception '@[app.periodLocked]%',
      jsonb_build_object('date', p_doc->>'doc_date')::text;
  end if;
end $$;
```

`p_op` називає дію словом застосунку: `insert`, `update`, `post`, `unpost`,
`delete` (позначка), `undelete`, `purge`. З `TG_OP` цього не видно — і
проведення, і позначка на видалення там `update`, — тому називає ядро.

**Свій тригер на `app.document` після цього можна прибрати.** Заразом зникає
пастка, на якій такі сторожі мовчки не спрацьовують: `new is not null` істинне
лише коли непорожні ВСІ поля рядка, а в шапці є nullable-колонки — тригер стоїть,
увімкнений, і не робить нічого.

Функція з іншим підписом валить запис із підказкою про очікуваний: мовчазний
пропуск тут гірший за відсутність гака — застосунок вважав би, що заборона діє.

**Порожній звіт малює база — своє «немає даних» у `<tbody>` більше не
показується.** `ReportBase` кличе `renderBody()` лише коли є дані, інакше малює
порожній стан сам і називає причину: звіт ще не формували («натисніть
„Оновити“») чи сформували й порожньо («немає даних **за відбором:** ТОВ «Демо» ·
Липень 2026»). Рядок відбору дає `emptyHint()` з умовчанням `printSubtitle()` —
той самий підпис, що вже йде на папір.

Приберіть із звітів рядки виду:

```ts
${rows.length === 0 ? html`<tr><td colspan="8">${t("common.noData")}</td></tr>` : ""}
```

**Перевірте `hasData` там, де порожній перелік рядків — це ще не порожній
звіт.** База вважає даними непорожній `$root.rows`; картка рахунку з вхідним
сальдо й без оборотів за період під це не підпадає, а відповідь у неї є. Тоді
`hasData` перекривається — заразом це вмикає кнопки друку й експорту:

```ts
protected override get hasData(): boolean {
  if (super.hasData) return true;
  const { openingDebit, openingCredit } = this.$root.totals;
  return Number(openingDebit) !== 0 || Number(openingCredit) !== 0;
}
```

---

## client 0.9.1 · tools 0.9.0 · skills 0.1.7 · create 0.7.0

### Потребує дії

**`lookup` навчився ссылкам — моделі з ссылкою в пікері треба перегенерувати.**
Раніше `<Model>LookupRowSchema` із вкладеним об'єктом (`counterparty`) давав
зламану функцію: генератор виводив `r_counterparty.name`, а `left join`, що
створює цей аліас, ставив тільки в `list`. `create function` тіла не перевіряє,
тож генерація й публікація були зелені, а пікер падав на першому відкритті:

```
ERROR: column t.counterparty does not exist
```

Тепер `lookup` бере ті самі join-и, що й `list`. Ссылка називається **ключем
вкладеного об'єкта**, а `x-ref` лишається на полі `<name>Id` в `ItemSchema` —
дублювати анотацію не треба й не можна:

```ts
export const InvoiceLookupRowSchema = Type.Object({
  id:           Type.String({ "x-db-type": "bigint" }),
  number:       Type.Optional(Type.String()),
  counterparty: Type.Object({ id: Type.String(), name: Type.String() }),
});
```

Прогоніть `deno task sql:gen <family>/<model>` для моделей, чий пікер показує
ссылку, і опублікуйте. Рукописний обхід (копія `_lookup` з дописаним join) після
цього можна викидати цілком.

**Пошук у пікері тепер шукає й по ссылках** — там, де в схемі стоїть
`searchable: true`. Доти та сама анотація діяла в списку й мовчки не діяла в
підборі. Якщо шукати по назві контрагента в пікері не треба — заберіть
`searchable` (він і в списку тоді зникне) або приберіть ссылку з
`LookupRowSchema`.

### Змінилося саме

**Esc над відкритим `<dialog>` більше не закриває вкладку під ним.** Оболонка
слухає клавіатуру на `window` і, впізнавши Esc, кликала `preventDefault()` —
тобто скасовувала закриття самого діалогу й закривала вкладку замість нього.
Виглядало це як «вікно не реагує на Esc», а справжня втрата була поруч: під ним
зникала вкладка з незбереженою формою. Правило «обробив — познач» тут не
працювало ніколи: нативне вікно закриває себе браузером, і позначати подію нема
кому. Тепер оболонка впізнає відкрите `<dialog>` у `composedPath()` сама —
власним діалогам застосунку робити для цього нічого не треба.

### Тільки для нових застосунків

**Діалог зміни власного пароля** — `app/header/change-password-dialog.ts` плюс
кнопка в шапці. Без нього пароль у застосунку змінювався рівно один раз і лише
примусово: форма в `app/login/` вмикається прапорцем `mustChangePassword()` і
гасне разом із ним, а більше до `changeOwnPassword()` не веде нічого. Наявним
застосункам файл треба взяти з шаблону — з ним і ключі `header.password*` у
`app/header/_locales/`.

---

## client 0.9.0 · server 0.11.1 · tools 0.8.0 · skills 0.1.6 · create 0.6.1

### Потребує дії

**Звіти: команда `index` тепер генерується.** `sql:gen` складає обгортку
`app.<report>_index` у `db/_generated/<report>.index.gen.sql` — розбір
`payload.filters`, відмову при незаповненому обов'язковому фільтрі, эхо
`$filters` і конверт. Рукописним лишається запит.

Наявні звіти **не ламаються**: збирач кладе ваш `db/<report>.sql` після
згенерованого файлу, тож ваш `_index` його перекриває. Але з'являється мертвий
згенерований файл і рядок `⚠` про відсутню `<Pascal>FiltersSchema`. Щоб перейти
на новий механізм:

1. оголосити походження ссылочних фільтрів — `x-ref` у схемі фільтрів;
2. звести обов'язковість: те, без чого звіт не будується, — без `Type.Optional`;
3. перейменувати `app.<report>_index(user_id, payload)` на
   `app.<report>_data(user_id, filters)` — фільтри приходять уже розібраними
   (ссылка згорнута до id: `organization` → `organizationId`);
4. прибрати з нього конверт, эхо `$filters` і перевірки обов'язкових — це тепер
   робить обгортка; повертати лише `{rows, totals, extra}`.

Подробиці — скіл `model-report-form`.

**`<ui-picker>`: умовчання `fetch` стало `lookup`** (було `fetch`). Явний
атрибут працює як раніше. Умовчання доти не працювало ні в кого — команди
`{model}_fetch` немає в жодної моделі, — тож ламається лише той випадок, де
команда справді зветься `fetch`: назвіть її явно.

**`@source`, що не веде в наявний каталог, валить збірку** — з іменем файлу,
номером рядка й підказкою. Раніше це мовчало, і класи, вжиті лише всередині
фреймворку, просто не потрапляли в CSS: кожен його екран виглядав не так, при
зеленій збірці. Найчастіша причина — перенесення рішення між деревами; у
встановленому застосунку правильний шлях:

```css
@source "../../vendor/jsr.io/@altera";
```

**`x-db-type` перевіряється за списком.** `numeric(10,2)` тепер помилка
генерації, а не мовчазний текстовий fallback, який падав аж на першому `save`.
Пишіть голий тип (`numeric`), точність задає DDL.

**Таблиця генерованої моделі мусить мати `created_at` і `updated_at`.** Це було
завжди — `save` пише `updated_at = now()` беззастережно, — але ніде не було
сказано; без них генерація й публікація зелені, а падає перший запис.

### Змінилося саме

**`type: register` отримує генерований CRUD** — `list`/`get`/`save`/`delete`, усе
як у довідника, але **без `lookup`** (на регістр ніхто не посилається), і
`<Model>LookupRowSchema` від нього не вимагається. Позначки на видалення
генератор не додає, якщо в схемі немає поля `isDeleted`, — для регістра це
правильно: позначений рядок мовчки псував би «зріз останнього». Власне читання
регістру (`_at`, `_history`, `_set`) лишається рукописним.

**Виміри субконто.** Опис довідника (`target_table`, `code_column`,
`name_column`) більше не переписується при кожній публікації — сід дає лише
умовчання, далі опис належить установці. Неіснуюча колонка в описі тепер
відхиляється тригером при записі, а не вилазить при проведенні документа
(`ERROR: column "code" does not exist`). Сід ставить лише ті виміри й аналітики
рахунків, чиї таблиці й рахунки в установці справді є. На базах, де вимір «Банк»
описаний колонкою `code`, якої в `app.bank` немає, міграція виправляє його на
`mfo` — але тільки якщо колонки `code` справді немає, а `mfo` є.

**Звіт відмовляє замість порожнього аркуша**, коли не заповнений обов'язковий
фільтр: конверт `ok:false` з прив'язкою до поля.

**`<ui-picker>` попереджає в консолі**, коли заданий `selected-id` без
`display-value` — це завжди означає, що форма не додала підпису, а на екрані
виглядає як «дані не прийшли».

### Тільки для нових застосунків

`deno task check` (перевірка типів усього `app/`) — у `deno.json` зі scaffold.
Наявним застосункам додайте рядок самі, він потрібен:

```jsonc
"check": "deno check ./app"
```

Вхід `app/main.ts` екранів НЕ перевіряє — їх вантажить оболонка динамічно, тож
розбіжність екрана з базовим класом після оновлення `@altera/client` не видно
ніде: збірка зелена, а кнопка мовчки зникає.
