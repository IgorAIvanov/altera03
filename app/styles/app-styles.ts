/**
 * Місце, де зібраний Tailwind потрапляє у фреймворк.
 *
 * `?inline` — фіча Vite, і живе вона тут разом із самим Vite: бібліотека такого
 * імпорту дозволити собі не може (JSR його не приймає, та й компілювати стилі
 * застосунку — не її справа). `tw` мутується, тому цей модуль можна імпортувати
 * будь-де в графі: компоненти, визначені раніше, підхоплять стилі самі.
 */
import raw from "./tailwind.css?inline";
import { setAppStyles } from "@client/shared/styles.ts";

setAppStyles(raw);
