{
  "//": "Порядок важливий: пакети ядра й моделі застосунку чергуються за FK. @core/document_core посилається на catalog/organization, currency і chart_of_account — додай його НИЖЧЕ за ці моделі, коли вони з'являться. Той самий порядок діє й усередині застосунку: довідник, що працює субконто, оголошує свій вимір сам (app.analytic_dimension), а план рахунків прив'язує вимір до рахунку — отже довідник стоїть ВИЩЕ за план. Ядро субконто не сіє: воно дає лише механізм. Запис admin/menu — це лише манифест і сід застосунку: структура й функції меню живуть у ядрі (@core/menu). @core/audit потрібен завжди: рантайм читає з app.audit_setting рівень журналу на кожній команді, а без таблиці друкує помилку в консоль (журнал при цьому вимкнений — умовчання рівня 'none').",
  "output": "{{name}}.app.sql",
  "outputs": {
    "structure": "{{name}}.struc_app.sql",
    "models": "{{name}}.models_app.sql",
    "migrations": "{{name}}.migration_app.sql",
    "data": "{{name}}.data_app.sql"
  },
  "models": [
    "@core/base",
    "@core/access",
    "@core/audit",
    "@core/remark",
    "@core/menu",
    "admin/menu",
    "@core/print_template",
    "catalog/counterparty",
    "@core/attachment"
  ]
}
