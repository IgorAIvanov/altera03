-- Фундамент: розширення й схема. Більше тут нічого немає навмисно —
-- користувачі, сесії, групи й права переїхали у власний пакет @core/access,
-- бо це вже підсистема з власним контрактом, а не «база».
create extension if not exists pgcrypto;
create schema if not exists app;
