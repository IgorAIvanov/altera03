{
  "name": "@altera/tools",
  "version": "0.13.19",
  "//": "Збіркові й дев-інструменти фреймворку: codegen SQL/реєстру, публікація, in-process клієнт застосунку. Знання про конкретний застосунок у пакет не зашите: codegen/publish отримують каталог аргументом, а AppClient — фабрику `createServer` інжекцією. Тонкі обгортки (api/smoke) живуть у застосунку й передають сюди свій createServer.",
  "//issue-access-token": "Видача персонального токена з консолі — потрібна там, де браузера немає (сервер без інтерфейсу, перша установка): сам ендпойнт видає токен лише по сесії. Коментар навмисно ПОРУЧ із exports, а не всередині: ключі мапи мусять починатися з крапки, і один \"//\"-ключ усередині робить невалідною всю мапу — резолвінг @altera/tools/* мовчки падає в усьому воркспейсі.",
  "exports": {
    "./generate-client-assets": "./generate-client-assets.ts",
    "./generate-app-locales": "./generate-app-locales.ts",
    "./scan-translation-markers": "./scan-translation-markers.ts",
    "./generate-scaffold-template": "./generate-scaffold-template.ts",
    "./verify-scaffold": "./verify-scaffold.ts",
    "./generate-core-sql": "./generate-core-sql.ts",
    "./generate-model-sql": "./generate-model-sql.ts",
    "./generate-model-runtime-registry": "./generate-model-runtime-registry.ts",
    "./issue-access-token": "./issue-access-token.ts",
    "./assemble-sql-package": "./assemble-sql-package.ts",
    "./publish-app-sql": "./publish-app-sql.ts",
    "./publish-sql": "./publish-sql.ts",
    "./export-solution": "./export-solution.ts",
    "./import-solution": "./import-solution.ts",
    "./solution-status": "./solution-status.ts",
    "./update-solution": "./update-solution.ts",
    "./app-client": "./app-client.ts",
    "./dev-guard": "./dev-guard.ts",
    "./set-password": "./set-password.ts"
  },
  "license": "MIT",
  "//publish": "Те саме правило, що в server: проби лишаються в репозиторії. У пакеті вони не потрібні (в `exports` їх немає), а `@std/assert` інакше їде в тарбол незадекларованим — рівно той різновид залежності, який JSR фіксує при публікації мовчки й назавжди.",
  "publish": {
    "exclude": ["**/*_test.ts"]
  },
  "//imports": "Як і в client: залежності пакета оголошені тут, а не успадковані з кореня воркспейсу — інакше версія, з якою пакет піде в реєстр, є побічним ефектом файлу застосунку. JSR до того ж вимагає обмеження версії: голий `jsr:@std/path` у коді валить публікацію.",
  "//@altera/server": "Оголошено ЯВНО, хоч і не було раніше: п'ять модулів імпортували сервер, у карті його не було, і JSR зафіксував при публікації те, що стояло у воркспейсі (@0.3) — мовчки й назавжди. Через це в застосунок приїжджали дві різні версії сервера. Тепер тут лишилися лише споживачі рантаймових дрібниць (configFromEnv, hashPassword, findProductionMarker); SQL ядра сюди більше не імпортується взагалі — його передає застосунок аргументом.",
  "imports": {
    "@altera/server": "jsr:@altera/server@^0.21.1",
    "@pdf-lib/fontkit": "npm:@pdf-lib/fontkit@^1.1.1",
    "@std/jsonc": "jsr:@std/jsonc@^1.0.2",
    "@std/path": "jsr:@std/path@^1.1.2",
    "@std/tar": "jsr:@std/tar@^0.1.10",
    "postgres": "npm:postgres@^3.4.5"
  },
  "compilerOptions": {
    "strict": true
  }
}
